[project]
name = "whisper-pipeline"
version = "0.5.0"
description = "Transcribe or translate audio files using the Whisper large-v3-turbo model on Apple Silicon."
requires-python = ">=3.12"
dependencies = [
    "mlx-whisper",
    "streamlit",
]

[dependency-groups]
dev = [
    "pytest",
    "ty",
    "ruff",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.ruff.lint.isort]
combine-as-imports = true

[tool.ty.environment]
python-version = "3.12"
