# CLAUDE.md

Streamlit application for transcription and translation using OpenAI Whisper on Apple Silicon with MLX.

## Setup

```bash
uv sync
uv run streamlit run streamlit_app.py
```

Python floor is **3.12** (`requires-python`), and `[tool.ty.environment] python-version = "3.12"` pins `ty` to it — so a 3.13-only stdlib symbol runs fine in the local 3.13 `.venv`, passes `pytest`, and fails `ty check` with an error pointing at `pyproject.toml`. There is no `.python-version`, so a fresh `uv sync` takes the newest interpreter uv can find that satisfies the floor, managed installs first — **3.14** on this machine at the time of writing, while the checked-in `.venv` was created on 3.13.12 and is what every `uv run` inside the repo still uses (`uv sync` reuses an environment whose interpreter still satisfies `requires-python`). So a fresh clone can land on a different minor than the existing venv; every "3.13" in this file describes that venv, not a property of the setup. CI pins `UV_PYTHON: "3.12"` instead, so the floor is exercised there and only there.

## Commands

- **Lint**: `uv run ruff check .`
- **Format**: `uv run ruff format .`
- **Typecheck**: `uv run ty check`
- **Test**: `uv run pytest`

When working with Python, invoke the relevant `/astral:<skill>` for uv, ty, and ruff to ensure best practices are followed.

When changing the Streamlit UI (tabs, widgets, theme, layout, caching, fragments), invoke the `developing-with-streamlit` skill to stay version-correct against the locked Streamlit version (currently 1.63.0; the floor is `>=1.59`). ⚠️ **The references it loads are not the skill's own — they ship inside the installed Streamlit wheel (`streamlit/.agents/skills/developing-with-streamlit/`), so they describe the *installed* version, never the floor.** The user-level skill is only a `discover.py` that resolves that path from the project's `.venv` (`$VIRTUAL_ENV` wins if set, so an unrelated activated venv loads *its* Streamlit's docs), which means every upgrade silently swaps the guidance. It once recommended `st.skeleton` against the old `>=1.58` floor; the 1.63.0 references recommend, without any version qualifier, `st.title/header/subheader(icon=)`, `@st.fragment(key=)` + `st.rerun("<key>")`, `st.status`/`st.expander(type="step")` and `runner.cacheBackgroundRefreshTTLMultiplier` (all 1.63.0-only), `wrap=` on containers/columns/buttons/toggles/multiselect (1.62.0) and on `st.segmented_control`/`st.markdown` (1.63.0), `refresh_mode="background"` (1.61.0), and `from streamlit.typing import UploadedFile` (1.62.0) — none of which exist at the floor, and nothing in the gate would notice, since the `Stop` hook runs the local `.venv` and CI runs `uv sync --locked`, both 1.63.0. The `streamlit.typing` one is an instruction, not a suggestion: the 1.63.0 `SKILL.md` and `api-reference.md` say to import `UploadedFile` from it and "not from modules under `streamlit.runtime`". `streamlit_app.py` and `tests/test_app.py` deliberately keep `streamlit.runtime.uploaded_file_manager` — the module is a `ModuleNotFoundError` at 1.59–1.61, the class is the same object either way, and `UploadedFileRec`, which the tests need, is not re-exported from it. Check any newly-recommended API — *parameters included*, since `hasattr` is satisfied by a command that exists but lacks the recommended kwarg — against the floor before using it: `uv run --no-project --with 'streamlit==1.59.0' python -c "import inspect, streamlit as st; print(inspect.signature(st.<name>).parameters)"` settles it in one command. Raise the floor deliberately rather than by accident.

## Automation

- **Hooks** (`.claude/settings.json`, checked in): a `PostToolUse` hook runs `ruff format` + `ruff check --fix` on edited `*.py` files and `ruff format` alone on edited `*.md` files (`ruff check` is a no-op on Markdown — it reports "No Python files found" and exits 0, so only the formatter is worth running there) — both branches sit behind a `case "$f" in "$PWD"/*)` containment guard, since the hook fires on *any* edit Claude makes and `cd "$CLAUDE_PROJECT_DIR"` pins only ruff's **config** discovery, not its **target**: without the guard an edit to `~/.claude/**` or a scratchpad file is reformatted with this project's `line-length = 100`; a `Stop` hook gates on `uv lock --check` + `ruff check` + `ruff format --check` + `ty check` + `pytest`, feeding failures back for repair (guarded against re-engage loops via `stop_hook_active`, and entered via `cd "${CLAUDE_PROJECT_DIR:-.}"` so an unset variable falls back to the cwd rather than `cd ""`-ing into a silent `exit 0` that skips the whole gate). Personal overrides live in the gitignored `.claude/settings.local.json`
- **The Stop hook deliberately duplicates the `PostToolUse` hook's `ruff` work.** `PostToolUse` ends in `> /dev/null 2>&1 || true`, so if `uv run ruff` ever breaks (venv rebuilt, `$CLAUDE_PROJECT_DIR` unset, or a path that the containment guard rejects) it silently stops formatting with no signal. Running `ruff` in the `Stop` gate too costs ~50 ms (measured: ~4.3 s for the full gate vs. ~4.3 s for `ty` + `pytest` alone — indistinguishable within run-to-run noise, with `pytest` ~4.0 s of it) and converts that silent failure into a blocking one. Do not "de-duplicate" it
- The two `ruff` steps in the `Stop` hook take `-q`; `ty check` deliberately does **not**. `ruff -q` suppresses only the success chatter and still prints full diagnostics on failure, but `ty check -q` collapses a real type error to the bare line `Found 1 diagnostic` — which, as the `reason` fed back for repair, names neither the file nor the problem. `ty check` also exits 1 on any `warn`-level diagnostic, not only errors (`--exit-zero-on-warning` is the opt-out, and the gate passes neither it nor `--error-on-warning`), so a warning blocks the `Stop` hook and CI exactly like an error — that is what turned the stale `ty: ignore` on `import mlx.core` into a red gate the moment the lock moved to an mlx release that ships stubs (see **Model**). Of the ten rules added between ty 0.0.69 and 0.0.80, three are on by default: `invalid-module-getattr-call` (error), `missing-slot` (error) and `redundant-condition` (warn); the last is the only one with a realistic surface here — an `if` whose operand is statically always truthy, e.g. a forgotten `()` on a function name
- **CI** (`.github/workflows/ci.yml`): runs `ruff check` + `ruff format --check` + `ty check` + `pytest` on push to `main` and on PRs. Pinned to a **`macos-14` (Apple Silicon) runner**, and the job sets `UV_PYTHON: "3.12"` — the only place the declared floor is actually exercised, since the local `.venv` runs 3.13. There is no `uv python install` step: `uv sync --locked` provisions the interpreter, and a bare `uv python install` would fetch uv's newest default instead of the pin. `enable-cache: true` on `setup-uv` is load-bearing rather than cosmetic — `mlx-whisper` depends on **`torch`**, ~520 MB installed at 2.14.0 (apparent size of `site-packages/torch` with `__pycache__` excluded; `du -sh` reads 542M once bytecode is compiled; ~500 MB at 2.13.0), so a cold cache dominates the run. It is install cost only — `mlx_whisper/torch_whisper.py` is its sole user and nothing imports that module, so neither the app nor the tests ever load `torch`
- **Every action is pinned to a full commit SHA**, with the version in a trailing comment (`actions/checkout@3d3c42e… # v7.0.1`). Tags are mutable, so a `@v7` that is force-moved or compromised runs arbitrary code holding the workflow's token — and the `release` job below carries `contents: write`. Use a SHA for any action added later, and bump the trailing comment with it
- **Dependabot** (`.github/dependabot.yml`) keeps those pins from going stale — a frozen SHA never surfaces the security fix it is missing. Weekly `github-actions` updates only (**not** `uv`/Python deps), grouped into a single PR and prefixed `ci:` to match the commit convention. It rewrites the SHA *and* its trailing `# vX.Y.Z` comment together, which is the property that makes SHA pinning maintainable. `directory: "/"` is required for this ecosystem — Dependabot scans `.github/workflows` on its own and does not take that path. Version updates need no repo setting; the config file's presence on `main` is what enables them
- **`macos-14` is required by the backend, not by wheel availability** — the older "`mlx-whisper` ships no Linux wheels" rationale is obsolete. `mlx_whisper` is a pure-Python `py3-none-any` wheel and `mlx` ships `manylinux_2_35` wheels that are already in `uv.lock`, so `uv sync --locked` resolves *and installs* fine on Linux. But `mlx` depends on its compute backend unconditionally only on Darwin (`mlx-metal`); on Linux the backend is opt-in (`mlx-cpu` / `mlx-cuda-*`, gated behind the `cpu` / `cuda` extras) and the lockfile carries neither. A Linux runner therefore goes green through `uv sync` and dies at `import mlx_whisper` several steps later. Switching to `ubuntu-latest` is possible by adding `mlx[cpu]`, but it would test a backend the app never ships on
- **Release** is the `release` job *inside* `ci.yml` (there is no separate release workflow; `draft-release.yml` was merged in). It reads `project.version`, then tags the commit and **publishes** `v<version>` with auto-generated notes. Runs on plain `ubuntu-latest` (unlike `check`) because it never calls `uv sync`, so the Darwin-only `mlx` backend never has to install
- ⚠️ **Bumping `version` in `pyproject.toml` publishes a public GitHub Release** — it tags `main`, marks the release Latest, and notifies watchers. A publish is not cleanly reversible: deleting it leaves anyone who already fetched the tag on a version that no longer exists. An incidental bump in an unrelated `pyproject.toml` edit therefore ships a release, so treat `version` as the release trigger it is. To stage one for review instead, drop `--draft=false` from the `gh release edit` line and publish by hand
- The release job's guard is the **release's existence**, not a `paths:` filter — it runs on every push to `main` and no-ops when `v<version>` is already published. A `paths:` filter misses a bump that arrives inside a merge commit and cannot be re-run after a failure; the existence check is idempotent and re-runnable. It resolves three states: `published` → exit, `draft` → publish it (a previous run died between the two `gh` calls; skipping would strand that draft forever), `missing` → create the draft, then publish. Anything else fails loudly rather than publishing a release whose state is unknown
- **`needs: check` is the point of the merge.** As a standalone workflow the release ran independently of CI, so a version bump drafted a release even when `ruff`/`ty`/`pytest` were red. Nothing may publish ahead of a green `check`
- Two details in the release job that look incidental and are not: `permissions: contents: write` is set **per job**, so the write scope never reaches the `check` job that actually runs third-party code; and `cancel-in-progress` is `${{ github.event_name == 'pull_request' }}` rather than plain `true`, because a blanket cancel can kill a `main` run mid-publish, between creating the draft and flipping it live
- **Creating the release as a draft first is load-bearing, not ceremony.** GitHub withholds the git tag while a release is a draft and creates it on publish, so a failure during note generation leaves no tag pointing at an unreleased commit

**The `Stop` hook mirrors every CI step.** `uv lock --check` is what stands in for `uv sync --locked`: `uv.lock` pins the project's own version, so bumping `version` in `pyproject.toml` needs `uv lock` in the same commit or CI fails. That used to be the one failure with no local signal.

**`uv lock --check` must run *first*, ahead of every `uv run` step — the ordering is load-bearing, not cosmetic.** `uv run` syncs the project environment before running its command, which **rewrites `uv.lock` in place**. So the gate's own first `uv run ruff` silently repairs the drift, and a `uv lock --check` placed after it can never fail. Measured both ways: with the check third in the chain, a bumped `version` passed the gate green and left `uv.lock` dirty in the working tree; moved to first, it blocks with `The lockfile at 'uv.lock' needs to be updated`. The step costs ~0.02 s and the five-step gate measures ~4.3 s (`pytest` ~4.0 s, `ty` ~0.2 s, the three `uv`/`ruff` steps under 0.1 s combined). That is up from the 3.17–3.25 s first recorded at `51d935a`, because the suite has since grown from 55 to 100 test functions (180 cases) — not because of any dependency change. The same property is why the local drift is easy to miss in the first place — any stray `uv run` fixes the lockfile on disk without staging it.

The gate mirrors CI's **steps**, not its **interpreter**: the hook runs against the local `.venv` (3.13) while CI pins `UV_PYTHON: "3.12"`. So a 3.13-only stdlib symbol passes the `Stop` hook's `pytest` and fails CI's. `ty check` is what closes that gap locally — `[tool.ty.environment]` targets 3.12 no matter which interpreter is running it — which is a reason not to drop it from the gate on the grounds that `pytest` already exercises the code.

Two `ruff` behaviors the gate now covers, worth keeping in mind rather than rediscovering:

- `ruff format` covers **Markdown** (0.16 reformats Python inside fenced code blocks in `.md`), so CI's `ruff format --check .` gates `CLAUDE.md` and `README.md`, not just the two `.py` files — `ruff format --check .` reports **4 files**
- selecting all of `E` enables `E501`, so an over-long line is a `ruff check` **error**, not something the formatter rewraps. But `E501` exempts a line that is a single whitespace-free token — a 124-character `x='<120 chars>'` (no spaces around `=`, so one token) passes `ruff check`, and `ruff format --check` catches it only incidentally, by normalizing the spacing and quotes: the same literal on its own line as `"<122 chars>"` passes **both** tools, and `x = '<120 chars>'` — the example this line used to carry — is three tokens and *is* flagged (measured at ruff 0.16.2 and 0.16.7). The gate runs both because each catches what the other cannot, not because the formatter backstops the exemption

## Code Style

- snake_case for functions/variables, PascalCase for classes
- Type annotations on all parameters and returns in `streamlit_app.py`; `tests/test_app.py` is deliberately unannotated (fixtures and mocks) and no `ANN` rules are selected
- `RuntimeError` for known transcription failures (no custom exception class)
- Import sorting via ruff with combine-as-imports
- `line-length = 100` (ruff's default is 88), `select = ["E", "F", "I", "W"]`

## Dependencies

- `mlx-whisper` — speech recognition on Apple Silicon
- `mlx>=0.24.2` — declared **directly**, not left transitive under `mlx-whisper`, because `streamlit_app` imports `mlx.core` itself to call `mx.clear_cache()` after each batch (see **Model**). It resolves to the same wheel either way; the declaration is what stops the import breaking if `mlx-whisper` ever stops depending on it. **The floor is a real API minimum, not caution** — `mx.clear_cache()` does not exist at 0.20.0 or 0.22.1 (both checked directly) and does at 0.24.2. 0.23.0 is untested because it ships no cp313 wheel, so the floor is the lowest version actually confirmed rather than the lowest that might work — **and it is an API floor only, not a typing one.** `ty` resolves `import mlx.core` through the `mlx/core/*.pyi` stubs most releases ship, against whatever `uv.lock` pins (0.32.2), never the floor; see the `import mlx.core` note under **Model** before re-locking `mlx` onto a stub-less release such as 0.32.0
- `streamlit` — web UI (floor `>=1.59`; developed and tested against the locked 1.63.0). **The floor is now a real API minimum, and it was not before.** It sat at `>=1.58` as a deliberate support boundary while the app in fact ran on 1.57.0 — `st.tabs(key=, on_change=)` and `TabContainer.open` (the class in `mutable_tab_container.py`) were already there, and so are `st.space`, `st.badge`, `st.segmented_control(required=)` and `text_alignment`. `st.skeleton` is the one thing that is not: it lands in **1.59.0** (absent in 1.58.0, bisected directly), so adding the fetch skeletons is what moved the floor. Changing this line means `uv lock` in the same commit — `uv.lock` pins the constraint, and the `Stop` hook's `uv lock --check` is what catches forgetting. `blinker` and `tenacity` left `uv.lock` with the 1.61.1 → 1.63.0 bump, not by a manual edit: 1.62.0 dropped `tenacity` (replaced by `streamlit/connections/retry_util.py`) and 1.63.0 dropped `blinker` (replaced by `streamlit/signal_util.py`); nothing here imported either
- `yt-dlp[default,deno]` — YouTube audio download, **with the extras that keep it off yt-dlp's deprecated JS-less client path.** YouTube changes its player clients continuously; without a JavaScript runtime yt-dlp falls back to a single JS-less client and warns on every fetch ("YouTube extraction without a JS runtime has been deprecated, and some formats may be missing") — a warning the app's former `"no_warnings": True` discarded. That option is gone on purpose: `"quiet": True` keeps yt-dlp's output off the UI, but its *warnings* now reach the server's stderr, and they are the only signal that a fetch fell back (a Deno that fails to exec, a solver script yt-dlp rejects); a normal fetch with the bundled runtime writes no warnings (measured: stderr carries only Streamlit's bare-mode notices; yt-dlp's progress bar on stdout predates this and is unaffected). `test_fetch_youtube_audio_uses_safe_options` asserts `"no_warnings" not in opts`. That path is not hypothetical: 2026.8.19 replaced the JS-less default client `android_vr` with `visionos` because YouTube 403'd every `android_vr` format from 2026-08-17 (yt-dlp PR #17461), and the 2026.7.4 lock this repo sat on until 2026-09-14 was still on `android_vr` — a live check that day found it serving a test clip again, so the JS-less client is *intermittently* broken rather than dead, but it saw 11 formats where the runtime path sees 24. The two extras close that: `default` brings `yt-dlp-ejs` (the JS challenge solver, plus brotli/mutagen/pycryptodomex), and `deno` installs a Deno binary (2.9.6, ~81 MB on disk, from a 38 MB `macosx_11_0_arm64` wheel that `setup-uv`'s cache holds like everything else) into `.venv/bin/`, which yt-dlp finds through `sysconfig.get_path("scripts")` with no `PATH` change — verified: `[debug] JS runtimes: deno-2.9.6`, no deprecation warning, same test clip fetches. yt-dlp enables only `deno` by default, so a Node on `PATH` would not have counted without `"js_runtimes": {"node": {}}` in `ydl_opts`; bundling the runtime is what makes a fresh `uv sync` sufficient. The `>=2026.8.19` floor is the lowest version confirmed to carry the `deno` extra, not a bisected minimum; it exists because uv only *warns* when a resolved version lacks a declared extra (`uv lock` exits 0), so without it a downgrade would shed the runtime silently. And `default` is what keeps `yt-dlp-ejs` pinned to the exact version yt-dlp vendors (`yt-dlp-ejs==0.8.0` in its metadata; `jsc/_builtin/ejs.py` skips a solver script whose major.minor differs, with a warning) — do not "simplify" it into a direct `yt-dlp-ejs` dependency, which would lose that lockstep the next time `--upgrade-package yt-dlp` runs. `test_yt_dlp_discovers_the_bundled_deno_runtime` pins the discovery itself (see **Testing**). What the extras do **not** fix is the rot itself: yt-dlp still tracks YouTube on a timescale of weeks, `tests/test_app.py` mocks `yt_dlp` at the boundary, the `Stop` hook and CI never touch the network, and Dependabot covers GitHub Actions only — its `uv` ecosystem (`package-ecosystem: uv`, manifest `uv.lock`) was considered on 2026-09-14 and deliberately not added, so YouTube breakage still surfaces as a user-visible `_error` first and a `uv lock --upgrade-package yt-dlp` second. The README troubleshooting entry says the runtime is bundled; changing this bullet means changing it too
- `ffmpeg` — audio/video decoding (system dependency)
- `ruff` — linting and formatting (dev)
- `ty` — type checking (dev)
- `pytest` — testing (dev)
- `watchdog` — event-based file watching for Streamlit's auto-reload (dev). Streamlit already
  depends on it, but declares the requirement as `platform_system != "Darwin"` — so on Apple
  Silicon it is *not* installed transitively, and `streamlit run` falls back to
  `PollingPathWatcher` (the source of the "For better performance, install the Watchdog module"
  notice). Declaring it here restores the `FSEventsObserver` backend. Dev-only because it affects
  the edit-reload loop, not transcription; the app is fully functional without it

## Architecture

- `streamlit_app.py` — single-file app entry point
- `tests/test_app.py` — unit tests
- `README.md` — user-facing docs, and the repo's largest concentration of hand-duplicated facts. It restates the accepted-format list, the 500 MB per-file cap, the Python floor, the `macos-14`/`mlx`-backend CI rationale, `ASR_MODEL_REPO` and its ~1.5 GB first-run weight download, the four dev commands, the `.srt`/`.txt` download switch, the 42-character subtitle wrapping, the YouTube troubleshooting entry (which says the JS runtime is bundled — see the `yt-dlp` bullet under **Dependencies**), and every Advanced-options control's description. So changing `AUDIO_FORMATS`/`VIDEO_FORMATS`, `server.maxUploadSize`, `requires-python`, `runs-on`, `ASR_MODEL_REPO`, or any control's `help` text means a README edit in the same commit. Nothing checks this — the runner rationale drifted silently and took `53b1f8d` to correct
- `docs/screenshot.png`, `docs/screenshot-result.png` — embedded in README with descriptive alt text naming the tabs and controls. Reshoot both, and update the alt text, on any UI or theme change; nothing checks them. See **Screenshots** below for the capture recipe

### Screenshots

`docs/screenshot.png` / `docs/screenshot-result.png` are **896 px wide** and script-captured rather than hand-shot, so the pair stays mutually consistent. The recipe, in case it needs re-deriving:

- **Playwright driving the installed Chrome** — `uv run --no-project --with playwright`, then `p.chromium.launch(channel="chrome")`. The default `chromium` launch fails asking for `playwright install` (the cached build predates the current release); `channel="chrome"` needs no download. The two obvious alternatives are both dead ends: `screencapture` needs a Screen Recording grant, and the Chrome MCP `computer` tool returns a downscaled JPEG — wrong for a PNG asset
- **Geometry**: viewport 1200×1600, `device_scale_factor=2`, `color_scheme="dark"`. Clip to the `.stMainBlockContainer` padding box plus ~3% per side, then downscale to 896 px wide (Lanczos) to match the README's existing image width. **The padding box is 736 CSS px, not 704** — 704 is the *content* box, and an earlier version of this line conflated the two. At a 1200 px viewport the element measures `x=232, w=736`, so the clip is `x = 232 − 0.03×736`, `width = 736 × 1.06 = 780`, which downscales to 896 from a 1560 px capture
- **That "plus ~3% per side" governs the horizontal clip only — do not apply it to the top by reusing `.stMainBlockContainer`'s own `y`.** The container's padding box carries ~96 px of `padding-top`, so clipping from it lands ~96 px of dead space above the title and does not match the checked-in assets. Anchor the top on the `h1` instead (`main.querySelector('h1')`, minus the same pad) and the bottom on the Transcribe button plus the pad. The two assets are **896×783** (input) and **896×596** (result); they are deliberately *not* the same height, only the same width
- **`docs/screenshot-result.png` needs a post-transcription state, and no `sample.wav` is checked in.** Rather than re-running the model, drive a scratch harness that seeds `st.session_state["transcription"]` / `["batch_id"]` and then `exec`s the app source with `st.status("Transcribed 1/1 file", expanded=False, state="complete")` textually injected immediately above the `st.fragment(_display_transcription)()` call. **That string has to track `_handle_transcription`'s real label**, which is built from `_plural` — it was `Transcribed 1/1 file(s)` until the `(s)` was fixed, and a harness left on the old text silently ships a screenshot of a label the app no longer produces. Injecting into an in-memory copy of the source is what puts the status widget at its real position in the layout — seeding alone renders the results without it, and the wrapper cannot emit it in the right place because its own code runs before the `exec`
- **Anchoring the result clip: three selectors bite.** `st.status` and `st.expander` share `data-testid="stExpander"`, so `querySelector` returns the **Advanced options** expander and the clip starts ~110 px too high, catching the Transcribe button. Select by text instead (`.find(e => e.innerText.includes('Transcribed'))`). And bound the bottom on the **Download button plus the pad**: walk up from the button until `borderTopWidth !== '0px'`, which terminates on the button itself, since `st.download_button` is a secondary button carrying its own `1px solid` border. That is what produced the checked-in **896×596** asset, and it is correct — the results card's border sits ~16 px below the button, inside the 22 px pad, so it lands ~7 px above the bottom edge rather than being shaved off. An earlier wording of this line claimed the opposite ("bounding on the button shaves off its bottom border") and prescribed a card-anchored bottom; walking from `download.parentElement` does reach the card and yields **896×615**, the height the asset had before `893e1d8`, but the asset was kept and the recipe now describes it. Start the walk from **Download** rather than any control in the settings card, since that walk finds the settings card instead. Re-verified at Streamlit 1.63.0: a fresh input capture is pixel-identical to the checked-in asset and the result capture differs only by sub-pixel anti-aliasing, so the 1.61.1 → 1.63.0 upgrade needed no reshoot. Clamp the top pad against the Transcribe button's bottom (`max(status.y − pad, midpoint)`) or the 3% pad reaches back into it. Third: **`innerText.includes('Transcribe')` also matches `Transcribed 1/1 file`**, so the lookup for the Transcribe button needs `&& !b.innerText.includes('Transcribed')` — the substring trap only exists in the result harness, where both strings are on the page at once
- **Viewport width no longer reframes the shot** (it used to). Every row now holds side-by-side on a narrow canvas: the `_labeled_toggle` rows via `_control_row()` (down to 420 px), and the **Transcribe** and **Download** rows via `st.container(horizontal=True, horizontal_alignment="right")` with a fixed `width=SELECT_WIDTH` (re-measured after the migration: the button stays 168 px right-flush at 1200 / 900 / 800 / 700 / 640 / 520 px). **Re-verified again after the controls moved into the bordered card**, which was the obvious thing for it to break — the card's 15 px padding narrows every row from 704 px to 672 px. All four labelled rows still hold side-by-side at 1200 / 900 / 800 / 700 / 640 / 520 / 460 / 420 px, and the selectbox and Transcribe stay exactly 168 px wide at every one of them. This was a hard "never shoot below ~700 px" rule while those two rows were `st.columns([3, 1])`, because the button went from 168 px to the full content width somewhere between 800 px and 640 px. Keep capturing at the documented 1200 px viewport anyway, so the pair stays consistent with the existing shots
- Hide `[data-testid="stToolbar"]`: the Deploy button and hamburger are browser chrome, not app UI
- Filling **Keyterms** leaves the multiselect popover open, and while open the combobox marks the rest of the app `aria-hidden` — dismiss it (Escape, then click elsewhere) or `get_by_role("button", name="Transcribe")` will not resolve
- Streamlit's `centered` container is `max-width: 736px` (**px**) while its type scale is in **rem**, so the two do not scale together. A capture taken at a non-default root font size reads proportionally larger; text that looks smaller than an older screenshot at the same canvas width is the standard 16 px root, not a regression

### Subtitle wrapping

`_format_srt` runs every cue through **`_wrap_cue`**, which wraps to `SUBTITLE_LINE_WIDTH = 42` — the broadcast convention (Netflix, BBC, EBU all land on 42 for Latin scripts). Whisper emits one segment per utterance with no regard for line length, so cues routinely ran well past it: valid SRT that every player accepts and that looks wrong burned into a picture. This is a **display** constraint, not a format one, which is why nothing but an explicit assertion catches a regression. Plain text is untouched.

Three details, each of which was wrong in a first draft:

- **Greedy wrapping alone is not enough.** `textwrap` fills each line to the brim and leaves a one-word orphan — more conspicuous on screen than the long line it replaced. `_wrap_cue` re-wraps at the *narrowest* width still producing the same line count, which spreads words evenly
- **`break_long_words=False` does not stop hyphen splitting.** `textwrap.break_on_hyphens` defaults to `True` and cut `https://example.com/an-extremely-long-path` in half mid-URL. Both flags are off, so the rule is simply "never split a token": an over-long token overflows its line instead
- ⚠️ **Do not add a "never narrow below the longest token" floor.** It looks obviously right and is measurably wrong — over 39k generated cues it changed 74, and *every* change was less balanced (`[24,24,22,21,30]` → `[24,24,29,14,30]`), while the over-long-token case it was meant to fix comes out byte-identical either way, because a token wider than `width` lands on its own line at every candidate width. The comment in the source records this so it does not get re-derived

The leading `" ".join(text.split())` is structural, not tidiness: a newline inside a segment would otherwise close the cue early and corrupt every cue after it.

**`_format_srt` also drops blank segments, and that is a separate defect from the wrapping.** mlx *clears* rather than removes them — "if a segment is instantaneous or does not contain text, clear it" (`transcribe.py`) sets `text` to `""` and then still extends `all_segments` with it. Emitting one produces an SRT block with an index, a timing line and **no text at all**, which is malformed and which strict parsers reject. It is reachable on *any* run, not only under **No verbatim**: the same branch fires whenever `start == end`. The filter sits before `enumerate` so cue numbers stay contiguous — skipping inside the loop would leave gaps in the sequence.

⚠️ **`_wrap_cue` is a no-op for CJK and that is not a bug to be fixed here.** `textwrap` breaks on whitespace, and Japanese and Chinese do not put spaces between words, so a CJK cue is a single token and comes back unchanged (measured: an 88-character Japanese line stays one 88-character line). That is no worse than before the wrapping existed, but it is worth knowing before assuming the feature covers all 100 languages the app advertises. Doing it properly needs character-level breaking against a different width — the convention is ~16 full-width characters, not 42 — so it is a separate feature, not a tweak to this one.

### Model

Direct `mlx_whisper.transcribe()` call with `ASR_MODEL_REPO = "mlx-community/whisper-large-v3-turbo"`. MLX accelerates natively on Apple Silicon. Results are cached via `@st.cache_data(show_spinner=False, max_entries=20)`; the spinner is disabled because per-file progress is rendered by the `st.status` wrapper in `_handle_transcription`.

`_transcribe` also pins `no_speech_threshold=0.6`, `logprob_threshold=-1.0`, and `compression_ratio_threshold=2.4`. These are **mlx_whisper's own defaults**, restated to keep the decode policy visible at the call site — not tuned values. Deleting one is a no-op that lets the library's default track upstream; changing one is a real behavior change, and `test_transcribe_calls_mlx_with_correct_params` asserts all three.

**`_handle_transcription` calls `mx.clear_cache()` in a `finally` around the batch**, and `mlx` is a **declared** dependency because of it (an undeclared direct import breaks the moment `mlx-whisper` stops pulling it in). MLX keeps freed device buffers on a free list rather than returning them, so a finished transcription leaves them resident for the life of the server process. Measured against `whisper-large-v3-turbo` with `word_timestamps=True`: **894 MB** held after an 8-second file and **1.25 GB** after two minutes, all of it reclaimed, for **4.2 ms**. Verified end to end — `peak` reaches 2444 MB during a batch and `mx.get_cache_memory()` reads **0.0 MB** once `_handle_transcription` returns. The next run costs 0.76 s against 0.74 s with the cache warm: it re-allocates rather than reloading, because the **model** lives in `active` memory (a flat **1543 MB** in every measurement) and `clear_cache()` does not touch it.

Three things about it that will otherwise get it reverted:

- ⚠️ **Verify with `mx.get_cache_memory()`, never `top`.** RSS moved **2.6 MB** while 894 MB was reclaimed, because these are device-mapped `MTLBuffer`s. Anyone checking the process's resident size concludes the call does nothing
- **`mx.set_cache_limit()` is the obvious cheaper alternative and is worse** — it caps the free list *during* decode, throttling the run it is meant to help. A batch boundary costs nothing in the middle of the work
- **`finally`, not a plain call after the block.** A tab switch — or any widget — raises `RerunException` mid-batch, and an interrupted run would otherwise hold its buffers until whenever the next transcription finishes, which may be never. `test_handle_transcription_reclaims_the_mlx_cache_when_interrupted` is what pins this, and it is the half that a success-path-only call still passes

Note `import mlx.core as mx` carries **no** `ty` suppression, and it used to. `mlx/core` is a compiled `.so`; `ty` introspects it only through the `mlx/core/*.pyi` stub package the wheel ships beside it (`__init__`, `fast`, `fft`, `linalg`, `metal`, `random`, `distributed`, `cuda` — in the cp312 wheel CI installs as well as the local cp313 one). **Whether a given release carries those stubs is a per-release packaging property, not a version floor.** Checked by installing each: present in 0.31.2 and 0.32.1+, absent in 0.32.0 — the release `5ce3bdb`'s dependency bump landed on, which is why `ea15fec` added `# ty: ignore[unresolved-import]` four days later and an earlier version of this paragraph generalised that one gap into "mlx ships no `.pyi`" (the lock had been on 0.31.2, which `ty` resolves unaided). The 0.32.1 release notes never say "stubs" — the change hides under *Refactor wheel building script (#3818)*, closing ml-explore/mlx#3916 — so bisect by installing rather than by reading notes. The directive is coupled to the locked version in **both** directions: on a stub-less release the bare import is an `unresolved-import` error and needs the line-scoped suppression back (never a `[tool.ty]` rule override, so a genuinely missing import still fails); on a stubbed one a *kept* directive is `unused-ignore-comment`, a warning, and `ty check` exits 1 on warnings — which is what turned the gate red when this upgrade moved the lock to 0.32.2. With the stubs `ty` checks the call for real: `def clear_cache() -> None` in `mlx/core/__init__.pyi`, and a wrong-arity call is an error. Two traps, one recorded in `pyproject.toml`'s `mlx` comment and one in the source comment above the import. The `>=0.24.2` floor in `pyproject.toml` is an `mx.clear_cache()` minimum, not a typing one — `ty` checks whatever `uv.lock` pins. And the comment above the import spells the directive **without** its leading `#`, because `ty` parses a `#`-prefixed `# ty: ignore[...]` anywhere inside a comment as live: mid-line it is reported as an unused suppression (exit 1), and on its own line directly above an import that genuinely fails to resolve it is *applied to that import* — `ty check` passes with exit 0 (measured at 0.0.69 and 0.0.80). That silent half is the real hazard; a bare `ty: ignore[...]` token with no `#` in front is inert. `mlx_whisper` imports it identically; `ty` only checks first-party code, which is why the same statement inside `site-packages` never raised anything.

Model weights are **not** wrapped in `st.cache_resource`. `mlx_whisper` already holds them on a process-level class attribute (`ModelHolder.get_model` in `mlx_whisper/transcribe.py`), so the repo is loaded once per server process; adding `st.cache_resource` would layer a second cache over an existing one.

### Fetch caching

`_fetch_youtube_audio` and `_fetch_url_audio` are `@st.cache_resource(max_entries=2, ttl="1h")`. These return **raw audio bytes**, so the two caches are the app's largest *worst-case* allocation: 2 caches × 2 entries × a 500 MB per-entry cap (`MAX_DOWNLOAD_BYTES`, now enforced on **both** paths — see below), i.e. ~2 GB. **This bullet used to say "dominant memory cost" and that was wrong** — it compared the fetch caches against nothing. Measured on this machine: the model weights are a flat **1543 MB** resident (`mx.get_active_memory()`, identical across every run), and MLX's allocator cache held a further **894 MB** after an 8-second file and **1.25 GB** after two minutes until `mx.clear_cache()` was added (see the **Model** section). Against a realistic fetch — an hour of YouTube audio-only Opus is tens of MB — the model, not the caches, is what the process is actually holding. The 2 GB figure bounds a tail nobody hits; treat it as a ceiling, not a description.

**`max_entries` was 5 and is deliberately 2, because it is the only lever on that ceiling.** `ttl` reads like the natural knob and is not one, for the reason spelled out below: it expires lazily, so it bounds *staleness* rather than resident memory. `max_entries` is what bounds how many payloads can coexist, and this is the one number that multiplies against a 500 MB cap. The cost is a re-download when a user cycles through more than two remote sources inside the `ttl` window — 2 still covers going back to the previous one, which is the common case. Note the realistic figure is far below the worst case (an hour of YouTube audio-only Opus is tens of MB, not 500), so this bounds the tail, not the typical run.

**`cache_resource`, not `cache_data`, and the deviation is deliberate.** `performance.md` scopes `cache_resource` to unserializable objects and `bytes` are plainly serializable, so the doc's default here is `cache_data`. The reason to deviate is resident memory: `cache_data` stores each entry **pickled** (`in_memory_cache_storage_wrapper.py` declares `TTLCache[str, bytes]`) and runs `pickle.loads` on **every hit** to hand back a fresh copy, and these fetches re-invoke on every rerun while their tab is open. That is three live buffers per active entry — the pickled entry, the per-rerun copy, and the separate `MediaFileManager` buffer backing the `st.audio` preview. `cache_resource` returns the *same* object and collapses the first two. It is safe here **only** because the return is a tuple of immutables; a shared mutable would be a cross-session aliasing bug, which is the hazard the doc's rule exists to prevent. Note this is a memory argument only — `pickle.loads` measures ~34 ms at the 500 MB cap and ~3 ms at realistic sizes, negligible against the fetch itself.

⚠️ **`cache_resource` lives in a different singleton store than `cache_data`**, so `tests/test_app.py`'s `_clear_caches` must call **both** `st.cache_data.clear()` and `st.cache_resource.clear()`. Verified directly: after `st.cache_resource`-decorating a counter function, `st.cache_data.clear()` leaves it cached and only `st.cache_resource.clear()` evicts it. Dropping that line makes the AppTest tab-gate cases order-dependent — the exact hazard documented under **Testing**. `max_entries` alone never evicts an entry that nothing displaces, which is what `ttl` addresses. **Both now pass `show_spinner=False`, which reverses an earlier decision, and the reversal is the interesting part.** They used to carry a `"Downloading audio from…"` message on the grounds that — unlike `_transcribe`, which is wrapped in an `st.status` — the cache spinner was the only progress signal on a slow download. An `st.skeleton` in each tab's reserved slot is now that signal, and the spinner actively fought it: **a spinner is an extra element with no reserved space**, so during a download it grew the slot by its own height and pushed the settings card and the Transcribe button down ~40 px, which then snapped back when it cleared. Reserving space is the entire point of the slot pattern, and the spinner was the thing breaking the reservation.

Measured, not reasoned about, against a local HTTP server trickling a real `.mp3` over ~6 s: with both, the settings card rendered stale-faded with the spinner text overlapping its top edge; with the skeleton alone the page does not move at all. The tradeoff accepted is that the skeleton is **wordless** — it says "something is loading here" but not what — which is judged the better failure, since the alternative was a visibly broken layout on every remote fetch.

The deferred fetch still runs inside `with youtube_slot:` / `with url_slot:` rather than writing to the container afterwards, and that is now what keeps the `st.audio` preview and both error alerts in the tab.

Be precise about what `ttl` does, though: Streamlit's `ttl_cache.py` expires **lazily** — expired entries read as absent, but are "only actually removed by a write, by `expire()`, or by a length/size query." So `ttl` bounds staleness and lets a later fetch reclaim the slot; it is *not* a background reaper, and a fully idle server can still hold expired payloads until something touches the cache. It shrinks the window, not the worst case.

**Both remote paths are now size-capped at `MAX_DOWNLOAD_BYTES`** (renamed from `MAX_URL_DOWNLOAD_BYTES`, which was accurate only while the URL path was the only capped one). The YouTube path needs **two** mechanisms and neither is redundant:

- `"max_filesize": MAX_DOWNLOAD_BYTES` in `ydl_opts` aborts the download early — but only sometimes. In the installed `yt-dlp` it is consulted at exactly two places, `downloader/http.py` (inside an `if data_len is not None:` branch, so it needs a `Content-Length`) and `downloader/external.py`. **No fragment/HLS/DASH downloader reads it**, which is precisely the multi-hour livestream VOD this exists to stop
- a `downloaded.stat().st_size` gate before `read_bytes()` catches whatever slipped through. An earlier version of this file claimed a stat check was impossible "since the download has already happened by the time the bytes are read" — that is backwards. The download having already happened is exactly *why* it works: what is being bounded is the slurp into a single `bytes` object and the cache entry holding it, not the disk write

`_transcribe` needs no `ttl` for the same reason it needs no size bound beyond `max_entries=20` — it caches result dicts (text + segments), not media. It also stays on `@st.cache_data`: it returns a nested dict, which is mutable, so the shared-object semantics that make `cache_resource` correct for the two fetch functions would be a hazard here.

### Input Modes

- `st.set_page_config(**PAGE_CONFIG)` is the first Streamlit call (before `st.title`); `PAGE_CONFIG` is a module constant (browser tab title `Whisper Transcribe`, a `:material/graphic_eq:` page icon, `centered` layout) so its values stay unit-testable
- **Upload** / **Record** / **YouTube** / **URL** tabs (`st.tabs`, each label prefixed with a Material Symbol icon — `upload`, `mic`, `smart_display`, `link`) — Upload accepts multiple files (`accept_multiple_files=True`) with one `st.audio` preview per file; the uploader's `type` is `AUDIO_FORMATS + VIDEO_FORMATS`, which Streamlit lists in the dropzone (no hand-maintained duplicate — but that line truncates, which is why the list is deliberately short; see **Accepted formats**); Record renders **no `st.audio` preview of its own** — `st.audio_input` is not a bare capture control but a full WaveSurfer player (interactive click-to-seek waveform, timecode, Play/Pause as soon as a recording exists, plus "Download as WAV" and "Clear recording"), so an `st.audio` call there stacked a second, visually different player on the same bytes. It is the one tab that needs no preview, and nothing in `AppTest` can catch a re-added one, since a Record tab with no recording renders no audio element either way; YouTube takes a URL (gated by a `youtube.com` / `youtu.be` regex and stripped of whitespace), downloads the best audio stream via `yt-dlp` (cached — see **Fetch caching** below — plus `restrictfilenames=True`, `noplaylist=True`, and a 500 MB cap via `max_filesize` *and* an on-disk `stat()` gate), and shows an `st.audio` preview of the bytes; URL takes any `http(s)` audio/video file URL (gated by an `https?://` regex; YouTube URLs short-circuit to an `st.info(..., icon=":material/smart_display:")` redirecting to the YouTube tab — the icon is the YouTube tab's own, so the callout points at its destination, and like `st.error` an `st.info` renders a bare box without one), downloads via `urllib.request.urlopen` with a 60-second timeout and a 500 MB cap (`MAX_DOWNLOAD_BYTES`, cached), derives the filename from the URL path (percent-decoded, fallback `download`), and shows an `st.audio` preview
- **Every `st.audio` call passes an explicit `format=`, and that argument is not optional decoration.** `format` defaults to `"audio/wav"` and is *not* a hint: it is handed verbatim through `_marshall_av_media` into `MediaFileManager.add()` with **no sniffing anywhere**, and becomes both the `Content-Type` the media route serves and the extension in the `/media/<hash>.<ext>` URL. Left at the default, an `.mp3` upload and a YouTube Opus stream were both advertised as WAV; Chrome and Firefox sniff the container and play anyway, but a browser that trusts the declared type can refuse — plausible on an app that only ships on macOS. `MEDIA_MIME_TYPES` is a hand-written extension→MIME map, deliberately **not** `mimetypes.guess_type`, which maps `.m4a` to the non-standard `audio/mp4a-latm` and varies by platform. It covers more than `AUDIO_FORMATS + VIDEO_FORMATS` because the YouTube and URL paths never consult those tuples. `DEFAULT_MEDIA_MIME` must stay non-empty — the media route does `media_type=mimetype or "text/plain"`, so an empty string would serve audio as text. The Record tab is the one place the `"audio/wav"` default is already correct, since `st.audio_input` returns WAV
- **The extension map is the *fallback*, not the first choice, on both remote paths — because a filename extension is the weakest evidence available and each fetch already holds something better.** Both now return `tuple[bytes, str, str]` (bytes, filename, mimetype) rather than deriving the type at the call site:
  - **URL — the server's own `Content-Type` wins** (`_url_mime`). The header is authoritative and covers precisely what an extension cannot see: a content-negotiated or query-driven URL (`/stream?id=42`), a redirect target with no extension, and the extensionless `download` fallback — all of which the map can only answer with `DEFAULT_MEDIA_MIME`, i.e. the exact `audio/wav`-for-everything mis-declaration this mechanism exists to remove. Only `audio/*` and `video/*` are trusted: a server answering `text/html` (an error page) or `application/octet-stream` must not get to set the declared type of an `<audio>` element, so those fall back to the extension
  - **YouTube — `info` distinguishes the container from its contents.** A `.webm` from `format="bestaudio/best"` is Opus with **no video track**, and declaring `video/webm` on an `<audio>` element is the same class of mis-declaration as `audio/wav`, not a fix for it. `info["vcodec"] == "none"` is the signal; `_media_mime(..., audio_only=True)` then maps through `AUDIO_ONLY_MIME_TYPES` to the `audio/*` sibling. A missing `vcodec` key deliberately falls back to the container type — the `best` half of `bestaudio/best` can select a muxed stream, and an unknown selection must not be *assumed* audio-only
  - A filename-extension map structurally cannot make either distinction, which is why this is a change to the fetches' return shape rather than a bigger table
- **Both remote tabs' placeholders are worked examples, not restatements of the label.** The URL tab's used to be the literal string `Audio/video file URL` — identical to its own `label_visibility="collapsed"` label, so the one tab whose input format is least guessable was the one that never showed you a valid value. It is now `https://example.com/audio.mp3`, matching how the YouTube tab has always used `https://www.youtube.com/watch?v=...`. Neither field takes `st.text_input(icon=)`, which exists at the floor and was considered: the active tab's own label already carries the icon directly above the field, so a second copy inside it is redundant. Nor does either take Streamlit ≥1.62's `st.text_input(type="url")`, the other route to that icon: it injects `:material/link:` by default (only `icon=""` opts out), sets `autocomplete="url"`, and adds a client-side regex whose scheme is *optional*, so it would accept `youtube.com/...` that the app's `^https?://` gates then silently drop; its sibling `validate=` withholds a non-matching value from the server, so `_error`'s messages would never render for that field. Both are above the `>=1.59` floor
- Under the title sits a one-line **`st.caption`** ("Audio and video, transcribed and translated locally on your Mac."), the app's only orientation text — a first-time visitor otherwise meets a bare title and a dropzone. `st.caption` and not `st.info` because `design.md` scopes the callout styles to instructions and problems and this is neither. It deliberately says *transcribed* locally rather than a flat "nothing leaves your Mac": the YouTube and URL tabs do reach the network, so the stronger claim would be false in two of the four modes
- Below the tabs, **every control lives inside a single `st.container(border=True)`**, grouped by intent (input → output → advanced). Always-visible, in order: **Primary language** selector (input), then the output group — **Translate to English** toggle, **Transcript format** segmented control, **No verbatim** toggle (enables `word_timestamps=True` + `hallucination_silence_threshold=2.0` to skip hallucinations on non-speech audio like music outros). **The two `st.space("small")` calls separating those tiers are the grouping**, and are not decorative padding: this section claimed a "grouped by intent" hierarchy for a long time while the rendered result was four identical rows in an undifferentiated run, because the grouping existed only as source ordering. The seams come in a pair — with only the first one the card reads as two tiers rather than three
- **The `Advanced options` expander is nested *inside* that card, and both halves of that are deliberate.** Semantically it is the third member of the input → output → advanced group, not a peer of it. Visually, leaving it outside put two identically-bordered boxes a few px apart and the card read as though it had been cut in half; inset by the card's padding the expander instead reads as subordinate. Verified by rendering both — do not "simplify" it back out to top level. The card itself reuses the `st.container(border=True)` that already wraps each result in `_display_transcription`, so settings and results read as the same kind of surface
- The three power-user controls live in that collapsed **`st.expander("Advanced options", icon=":material/tune:")`** (progressive disclosure — defaults still apply when closed; **do not** add `on_change="rerun"` + `if expander.open:` lazy gating here, see below): **Decode segments independently** toggle (sets `condition_on_previous_text=False` so each 30 s window decodes without prior-window context — robust on noisy audio at the cost of cross-boundary fluency), **Time range** text input (forwarded as `clip_timestamps`; comma-separated `start,end` pairs in seconds, e.g. `30,90` or `0,60,120,180`; blank → `"0"` for the full file; validated by `_validate_time_range` — malformed input disables the Transcribe button until corrected, with the `st.error` rendered outside the Advanced options expander *and* outside the card (above the button) so the disabled reason stays visible even when the expander holding the input is collapsed), and **Keyterms** chip input (`st.multiselect` with `accept_new_options=True`, max 50 chips, joined with `, ` and forwarded as `initial_prompt`). Below everything, a **Transcribe** button with a `:material/graphic_eq:` icon, alone in a `st.container(horizontal=True, horizontal_alignment="right")` at `width=SELECT_WIDTH`. `Transcript format` stays in the always-visible output group (never the expander) because it has a user-visible side effect — it flips the download between `.srt` and `.txt`
- **`Transcript format` is an `st.segmented_control`, and was an `Include subtitles` toggle.** It governs two things at once — what the results text area shows (plain text vs timestamped SRT cues) and which extension **Download** serves — so a boolean named one of the two states and left the other implied, reachable only through the help tooltip. The options come from `TRANSCRIPT_FORMATS = (FORMAT_PLAIN_TEXT, FORMAT_SUBTITLES)`, order is display order, and `include_subtitles` is derived once as `transcript_format == FORMAT_SUBTITLES`; everything downstream (`_transcription_kwargs`, `_handle_transcription`, `_display_transcription`) still takes the bool and is unchanged. **`required=True` is load-bearing and untestable.** Per the docstring it stops a user deselecting the chosen option ("clicking an already-selected option does nothing"); without it a single-select control returns `None` on deselect, which reads as plain text through that comparison while *looking* like nothing is selected. `AppTest` cannot see it — `ButtonGroup.unselect()` is a no-op for a single-select group with or without it, verified by deleting the argument and re-running — so this is a genuine silent-regression hole, not one a test will catch. **It is also the one right-hand control that does not take `width=SELECT_WIDTH`**: `st.segmented_control` already defaults to `width="content"` and measures ~173 px for these two labels, so pinning it to 168 would clip rather than align. It does not need to — `_control_row()`'s `distribute` right-aligns it, so it shares the selectbox's right *edge* (936 at a 1200 px viewport) without sharing its width. Re-measured across 1200/900/800/700/640/520/460/420 px: all four rows stay side-by-side and the right edges stay flush
- **Time range is an inline `_control_row()` at `width=SELECT_WIDTH`, not a `_field_label` with a full-width input stacked under it.** That makes it match the toggle above it and the four rows in the card, leaving **Keyterms** as the only stacked control in the app — which is correct, since its chips genuinely need the full width. The cost is that the placeholder had to shrink from `e.g., 30,90 (leave blank for full file)` to `e.g., 30,90` to fit 168 px; the dropped sentence is not lost, the `help` tooltip already carries it. `_field_label` therefore now has exactly one caller
- Every labelled control row shares one layout, defined once in **`_control_row()`**: `st.container(horizontal=True, horizontal_alignment="distribute", vertical_alignment="center")`, label first via `st.markdown(label, help=...)`, control second with `label_visibility="collapsed"`. Build rows through `_labeled_toggle(label, help_text)` or `_field_label(label, help_text)` (label row only, for a widget that renders itself below it) — **never a bare `st.toggle`**, and help text hangs off the label markdown, not the widget's own `help=`. The **Primary language** and **Time range** rows use `_control_row()` directly, since neither a selectbox nor a text input is a toggle; both pass `width=SELECT_WIDTH` so the control half matches the toggles' right edge. Neither helper has test coverage, so a bare widget breaks the grid silently
- **A horizontal container, not `st.columns([3, 1])` — the ratio was never load-bearing for these rows, only the flush-right edge was.** `horizontal_alignment="distribute"` is space-between with two children and plain left-alignment with one, which is why `_field_label` can share the identical row without a discarded empty column. The payoff is that columns stack vertically on a narrow viewport and a horizontal container does not; measured against an A/B app, `st.columns([3, 1])` breaks between 800 px and 640 px while `_control_row()` stays side-by-side down to 420 px
- `SELECT_WIDTH = 168` exists because **`st.selectbox` has no `width="content"`** — its default is `"stretch"`, which fills the whole horizontal container. 168 px is what `st.columns([3, 1])` used to yield and is measured, not guessed: the `centered` layout's main block has a 704 px content box and `st.columns` puts a 32 px gap between the two columns, so `(704 - 32) / 4 = 168`. The **Time range** input and the **Transcribe** and **Download** buttons now take that same constant via `width=SELECT_WIDTH`, so it is the *single source* of the shared **width** rather than a number that has to be kept equal to whatever a column ratio happens to yield. **It is not a shared right *edge*, and an earlier version of this line said it was.** Re-measured in the running app at a 1200 px viewport, after the always-visible controls moved into a bordered card: the selectbox and **Download** both land at `x=768, right=936, w=168`, while **Transcribe** lands at `x=784, right=952, w=168`.

**The split is exactly "inside a bordered card" versus "not", and that is the whole rule** — it is not a per-widget quirk to be memorized. A `st.container(border=True)` has computed `padding: 15px` plus a `1px` border, insetting its content box from 704 px to 672 px. The selectbox sits in the controls card and Download sits in `_display_transcription`'s results card, so both inset by 16 px. Transcribe sits outside both and stays flush with the 704 px content edge — which is also where the controls card's own right border falls, so the button lines up with the **card** above it rather than with the selectbox inside it. That is the correct look and is not removable without custom CSS, which this app does not use — do not "fix" the alignment by dropping `border=True` or hand-padding.

Note the pairing has now inverted twice, which is why the measurement is restated rather than reasoned about. It was selectbox+Transcribe at 952 with Download alone at 936; it is now selectbox+Download at 936 with Transcribe alone at 952. The original stale claim was written in `8b165dd` and invalidated by `aae209d`, the very next commit, which introduced the results container and simultaneously moved Download off `st.columns([3, 1])` + `width="stretch"` (which genuinely did yield `right=952`) onto `width=SELECT_WIDTH`. **Re-measure rather than infer after any change that adds or removes a bordered wrapper** — `scripts` for this live nowhere, but the check is a one-liner in the browser: `document.querySelector('[data-testid="stSelectbox"]').getBoundingClientRect()`. **Since 1.62 (`st.button`/`st.download_button`/`st.toggle`/`st.multiselect`) and 1.63 (`st.segmented_control`) every one of these controls takes `wrap=`, and its default `None` resolves in the frontend to "no wrap inside a horizontal container"** — a button label truncates with an ellipsis instead of wrapping and growing the button, a segmented control scrolls instead of wrapping, and a button with no `help=` gets a native `title` tooltip carrying its label, set unconditionally rather than only on overflow. In this app that means **Transcribe** now shows a browser tooltip reading `Transcribe` on hover (it has no `help=`; **Download** has one, so it does not) and nothing else changes — re-measured at 1.63.0, every rect above is within 0.4 px and all four rows hold side-by-side at every viewport width listed. Do not pass `wrap=` anywhere to "fix" that: it does not exist at the `>=1.59` floor, and a longer label belongs in a wider `SELECT_WIDTH`, not a taller button
- **`horizontal_alignment` on those two button containers must be `"right"`, never `"distribute"`.** Per the installed docstring, "a standalone element is aligned to the left" under `distribute` — so `distribute` silently unsticks the right edge for a single-child row (it is correct in `_control_row()` only because those rows have two children). `test_display_transcription_right_aligns_download` pins the value, and is mutation-checked
- **There is no longer any `st.columns` in `streamlit_app.py`.** An earlier version of this file justified keeping `st.columns([3, 1])` on the two button rows with "(a button *does* need a width ratio)". That was false when written: `st.button` and `st.download_button` both accept `width` as an int pixel value, in the locked 1.63.0 *and* well below the declared `>=1.59` floor. Do not reintroduce the split — `references/best-practices.md` in the bundled Streamlit skill names `left, right = st.columns(2); right.button(...)` as its BAD example against exactly this construct
- **The Transcribe button dispatches from the tab that is open**, falling back to the priority order uploaded files → recording → YouTube audio → URL audio when that tab has no source of its own. Each non-upload source is wrapped in a single-element list, and `tab_sources` pairs each with its `TabContainer` so `tab.open` can pick the winner. **It was a flat `or`-chain and that was a live mis-dispatch bug, not a style question.** Upload and Record declare their widgets in ungated tab bodies, so their values are sticky across tab switches, while `youtube_audio` / `url_audio` exist only while their own tab is open — so an earlier upload silently outranked the URL whose preview was on screen: the fetch ran, the player rendered, the button enabled, and **Transcribe** transcribed the upload. Three things hid it: both previews render at once, so two plausible sources are visible; the results `st.subheader` is the only signal of which one ran and it arrives after a full model pass; and `_transcribe`'s cache makes the second click return instantly under a fresh `batch_id`, which reads as "the URL happened to produce the same text" rather than "the app ignored it". The `or`-chain and a priority dispatch were the *same thing* until the tab gating made two sources able to coexist — which is why nothing looked wrong when it was written. **The fallback leaves a deliberate residue**: an open-but-empty Record tab with an earlier upload still loaded transcribes the upload. That is preferred over letting **Transcribe** go dead while a usable source exists, and it is the reason the fix is scoped as "mis-dispatch" rather than "only the open tab counts". YouTube and URL sources share a `_RemoteAudio` adapter exposing `.name` (a safe filename, including extension when available) and `.read()` so they flow through `_handle_transcription` without changes. UI flags are routed through `_transcription_kwargs`, which centralizes the `translate → task` mapping and the `decode_independently → condition_on_previous_text` inversion so a script-level negation can't silently disappear. Subtitles controls both the text area's initial content (SRT-formatted segments when on, plain text when off) and the format the **Download** button serves (`.srt` vs `.txt`); the text area is always editable
- `_transcribe` writes audio bytes to a temp file, calls `mlx_whisper.transcribe()` **once per clip**, and caches results (`language=None` → Whisper auto-detects). **The per-clip loop works around a real upstream bug: `mlx_whisper` does not honour more than one clip.** Its decode loop is `for seek_clip_start, seek_clip_end in seek_clips:` wrapping `while seek < seek_clip_end:` — `seek_clip_start` is bound and **never read**, and nothing assigns `seek = seek_clip_start`, so `seek` carries over from the previous clip and each later range continues from wherever the last one stopped. Given `0,60,120,180` — the README's own example — it decoded 0–180 straight through, including the 60–120 the user excluded. (`clip_idx` at `transcribe.py:247` is initialised and never incremented, vestigial from the same loop upstream.) A **single** pair was always correct, because `seek` is initialised to `seek_clips[0][0]`; only the multi-clip tail was broken, which is why it outlived a green test named `multi_clip` that asserted the string was *forwarded* — forwarding was never the broken half. `_split_clips` turns the string into one `start,end` string per clip (a trailing unpaired start stays open-ended, matching mlx's own reading) and `_merge_transcriptions` concatenates the results. Segment timestamps are **already absolute** — mlx derives them from `seek`, which starts at the clip's own start frame — so segments concatenate with no timestamp fix-up (the merge needs no change to `_format_srt` — which is separately edited by the subtitle-wrapping work, for an unrelated reason); `language` comes from the first clip, and the clips **cannot** disagree: with `language=None` mlx detects from `pad_or_trim(mel, N_FRAMES)` — the first 30 seconds of the *whole file*, not of the clip — so every call scores the same window. An earlier draft of this line and of the docstring claimed independent per-clip detection; that was wrong. `id` **is** renumbered across the merge, because mlx numbers segments per call and a plain concatenation would carry duplicates. **A single clip takes exactly one call and its result is returned unmerged**, so the common path is unchanged. One consequence worth stating: `condition_on_previous_text=True` no longer carries decoding context *across* clips, because each clip is now its own call. That is the right semantics for disjoint ranges — the previous behaviour only carried context because it was wrongly decoding straight through the gaps — but it is a behaviour change beyond the bug fix, not just a bug fix. The accepted cost on the multi-clip path is one ffmpeg decode of the whole file per clip; clips are typically two or three, and decoding what the user asked for beats one shared decode of what they excluded. The empty-text guard applies to the **merged** text, so one silent clip does not fail the file
- `_handle_transcription` wraps the batch in `st.status(...)` — opening at `Transcribing {n} file(s)…`, updating to `Transcribing {name} ({i}/{total})...` per file, and transitioning to `complete` with `Transcribed {done}/{total} file(s)` at the end. **The two batch-level labels count through `_plural(count, noun)` rather than a literal `file(s)`.** That parenthesised form is exactly wrong in the single-file case, which is the common one, and it rendered on every run; `_plural` is three lines and returns `1 file` / `2 files`. Neither batch label was asserted anywhere before, which is why the `(s)` survived as long as it did — `test_handle_transcription_pluralizes_the_status_labels` now pins both, parametrized over a single-file and a two-file batch, and is mutation-checked (reverting either label to `file(s)` fails both cases). It transcribes each upload and stores the resulting list of `{result, file_stem, filename, include_subtitles}` dicts in `st.session_state["transcription"]`. The list is published **before** the loop and re-published after each file, never only at the end: Streamlit interrupts a running script at the next ForwardMsg (`status.update()` is one) by raising `RerunException`, a `BaseException` that the `except Exception` handler deliberately does not catch. A single post-loop assignment would therefore throw away every file already transcribed whenever any rerun lands mid-batch — a tab switch, a toggle, any widget. The pre-loop publish additionally clears a previous batch when nothing in this one succeeds. Alongside that publish it bumps `st.session_state["batch_id"]`, which namespaces `_display_transcription`'s widget keys — see below for why a batch-invariant key is a correctness bug, not a cosmetic one. The `file_stem` includes the source extension (e.g., `interview_mp3_transcript`) to disambiguate downloads when two uploads share a stem. Per-file errors don't stop the rest of the batch, and they are **collected into a `failures` list and replayed after the `with st.status(...)` block, never rendered inside it.** Writing them inline was a real defect: `st.status` collapses on its *first* `status.update(label=...)` — `update()` clears the proto's `expanded` field unless it is passed again (`mutable_status_container.py`), and the frontend's label-change branch then resets the open state to that now-false backend value — so every alert landed inside a collapsed container carrying a *success* indicator. A failed file rendered a green check, `Transcribed 0/1 file`, and no visible explanation anywhere on the page. **Be precise about the failure mode: the status stays expandable, so the text was reachable** — what made it a defect is that nothing on screen suggested a failure had occurred or that anything was hidden, so no user would think to expand a green-checked status. Note `expanded=True` on the opening call does *not* save it and is not the fix; the collapse happens on the first per-file label update, which is unconditional. The terminal update also passes `state="error" if failures else "complete"`, since a batch that lost a file is not complete. A `BaseException` (`RerunException`) unwinds past the replay loop without rendering anything, which is correct — that run's output is discarded wholesale. **Every interpolation of `uploaded_file.name` goes through `_escape_markdown`** — hoisted once per iteration as `name_md` for the `st.status` label; the two failure messages instead carry the **raw** name and are escaped whole by `_error` when they are replayed, which is the point of routing everything through that helper. See the `_escape_markdown` bullet below for the two mechanisms it applies and the two sinks that need the second one. **It also `seek(0)`s the file before reading it**, guarded on `isinstance(uploaded_file, UploadedFile)` since `_RemoteAudio` has no cursor: `UploadedFile` subclasses `io.BytesIO`, and `read()` leaves its cursor at EOF. **The guard covers a same-object double read within one run, and an earlier version of this line claimed more than that.** It said the deserialized widget value survives every rerun as the same object, so a second **Transcribe** on an unchanged recording would hand `_transcribe` `b""` once the Record tab stopped rendering the `st.audio` preview whose `_marshall_av_media` had been rewinding it. That mechanism is wrong at 1.61.1 and 1.63.0 alike: `WStates.__getitem__` does cache `Value(deserialized)`, but `register_widget` hands the script `deepcopy(widget_value)` (`session_state.py`, "so that reference types can't be accidentally mutated by user code"), so the cached original is never read and every run — browser-initiated or `st.rerun()` — receives a fresh copy at cursor 0. The `seek(0)` stays because it costs nothing and pins the one real hazard, reading the same object twice in one run, which is what `test_handle_transcription_rewinds_the_cursor_before_reading` constructs. Filenames are untrusted input: `_fetch_url_audio` percent-decodes them off the URL path, so `%5B` / `%28` arrive as live Markdown syntax. The status label takes the Markdown label subset, which **includes images**, so an unescaped `![](https://host/x.png)` in a filename would fire an outbound request on the *happy* path — the label renders for every file, not just a failure — and `st.error` renders **full** Markdown, a strictly larger subset than the label subset `_escape_markdown` was originally written for. `name = Path(uploaded_file.name)` deliberately keeps the **raw** name: the suffix feeds the temp file and the stem feeds `file_stem`, and neither is a display surface
- **`_escape_markdown` covers *inline* parse-time constructs only — do not read it as sanitizing everything.** `:red[…]` / `:badge[…]` are micromark *textDirectives*, resolved during parsing, so a backslash suppresses them and the escape works. Three of Streamlit's extensions instead run **outside the parse**, where a backslash has already been consumed and the escape is a no-op: `:material/…:` (the frontend does `replaceAll(":material/", ":material_")` on the **raw** string *before* parsing, then substitutes over already-parsed text nodes), `:streamlit:` (same find-and-replace pass), and the typographic substitutions `-- -> <- <-> >= <= ~=` (a `visit` pass over text nodes; `-` is not in the escape set at all, and those regexes are whitespace-bounded, so `session 1 -- part 2.mp3` substitutes while `a--b.mp3` does not). So `session 1 -- part 2.mp3` still renders an em dash and `clip:material/delete:.mp3` still renders a trash icon. **Accepted, not fixed:** the impact is a mis-rendered heading — icons are a local font and the logo a bundled asset, so there is no injection and no outbound request — and the only fix is inserting zero-width spaces into displayed filenames, which is worse than the symptom. The image/link vectors, which *do* reach the network, are parse-time and *are* escaped
- **Two gaps that used to hide behind that "parse-time only" framing, both now closed.** Stating the limitation as a *rule* invited checking the list rather than the boundary, and both of these are parse-time constructs the rule implied were already covered:
  - **`&` is in the escape class.** `characterReference` is a genuine parse-time micromark construct, so without it `&amp;` / `&#38;` / `&#x26;` decode during parsing — which both breaks the literal display (`Rock &amp; Roll.mp3` showed as `Rock & Roll.mp3`) and, worse, decodes *before* the post-parse text-node passes above, so `clip&#58;streamlit&#58;.mp3` produced a live `:streamlit:` and rendered the logo in `st.subheader`, the `st.status` label, **and** `st.error` alike. `&` is ASCII punctuation, so `\&` is a valid CommonMark characterEscape and suppresses entity parsing outright. Still no injection: entity-decoded characters are never re-tokenized, so `x&#42;bold&#42;.mp3` renders literally and no new link/image syntax can be built this way
  - **Block markers are handled by collapsing whitespace *inside* `_escape_markdown`, not by the character class and not at a call site.** The function's last line is `_MARKDOWN_ESCAPE_RE.sub(r"\\\1", " ".join(text.split()))`. `#`, `>`, `-`, `+`, `|` and ordered-list `N.` / `N)` are deliberately **not** escaped; every block construct needs a *line start*, so removing newlines defeats all of them at once, which is a smaller change than growing the class. **Two** sinks need it, which is why it cannot live at one call site — `isLabel` is what turns on both the automatic `^(\s*)((?:[+\-*]|#+)(?=\s|$)|>)` escape and the `disallowedElements` strip list (`h1`–`h6`, `ul`, `ol`, `li`, `hr`, `blockquote`, `table`), and two renderers do not set it:
    - **`st.error`'s body.** `AlertElement` passes `isLabel:true` for the alert *title* and omits it for the *body*.
    - **`st.subheader`.** This one is genuinely counter-intuitive and an earlier version of this file got it backwards, asserting the heading was "hardened for free". It is not. The heading component does `let [first, ...rest] = body.split("\n")`, renders `first` as `## {first}` with `overrideComponents` — and then renders the remainder with `rest.length > 0 && l(Nt, {source: rest.join("\n"), allowHTML: false})`: a bare `StreamlitMarkdown` with **no `isLabel` and no `disallowedElements`**. So everything after the first newline in a heading is full Markdown, exactly like an alert body. Verified in the 1.61.1 bundle and re-verified at the installed 1.63.0, where the remainder render gained one gate — `!(wrap === false) && rest.length > 0 && StreamlitMarkdown({source: rest.join("\n"), allowHTML: false})` — because 1.63.0 added `wrap: bool = True` (and `icon=`) to `st.subheader`. `wrap=False` drops everything after the first newline but is **not** a substitute for the whitespace collapse in `_escape_markdown`: it is above the `>=1.59` floor, it is frontend-only (the proto still carries the newlines), it also ellipsizes the heading under `white-space: nowrap`, the first line is still rendered without `isLabel`, and `st.error` gained no `wrap` at all. The app passes neither, so the default path is the one described here. The Python half confirmed through `AppTest` — the subheader proto body arrives as `'clip\n\n# Big\n\n> quote.mp3'` verbatim
    Reachable at both because `_fetch_url_audio` percent-decodes, so `%0A` arrives as a real newline and the *raw* name is what `_handle_transcription` stores in the transcription dict: a URL ending `/clip%0A%0A%23%20Big%0A%0A%3E%20quote.mp3` used to render an `<h1>` and a blockquote inside the error box **and** inside the results card
- `_display_transcription` renders one stacked section per stored result, each wrapped in a `st.container(border=True)` (a flat stack put one file's **Download** button flush against the next file's heading; no-op with a single file): `st.subheader(filename)` (the filename is Markdown-escaped via `_escape_markdown` — otherwise `_`, `*`, brackets, or `:red[…]`/`:badge[…]` directives in names like YouTube titles would mis-render. **Do not describe this sink as "the Markdown label subset"**: only the heading's *first line* is rendered that way, and everything after a newline goes through a bare `StreamlitMarkdown` with full block Markdown — see the `_escape_markdown` bullet above, which is why the whitespace collapse lives in the helper rather than at a call site) + an editable text area (plain text or SRT segments per `include_subtitles`) + a **Download** button with a `:material/download:` icon that captures the text area's edited content. The button sits alone in a `st.container(horizontal=True, horizontal_alignment="right")` at `width=SELECT_WIDTH` — the same construct and the same constant as the Transcribe button — so the two share a **width**. They do *not* share a right edge: the bordered card insets Download by 16 px, per the `SELECT_WIDTH` bullet above. It also passes **`on_click="ignore"`**: the default is `"rerun"`, and nothing in this fragment depends on a post-download rerun (the payload is the text area's already-committed return value), so the default costs a fragment round-trip for no state change. The label is always `Download` regardless of format; the file extension (`.srt` when subtitles are on, `.txt` otherwise) is set via the filename + MIME type args, and a `help=` tooltip explains the format switch on hover. **That tooltip's second sentence — "Commit an edit first … or the download will miss it" — is a mitigation for a real defect, not filler.** `st.download_button` materializes non-callable `data` into the `MediaFileManager` at *render* time and hands the frontend a pre-baked URL that it fetches synchronously on click, while `st.text_area` commits only on blur or Ctrl/Cmd+Enter. So clicking **Download** with an uncommitted edit serves the *previous* text. Verified end-to-end in real Chrome: typing into the box and clicking once downloaded the pre-edit string; the click's own blur committed the edit and a second click got it. It is **not fixable in-app** — `st.form` raises `StreamlitAPIException` on a download button, and a deferred callable `data` is invoked at click time *before* the pending widget update lands — so the tooltip and the README troubleshooting entry are the fix. Changing this string means updating `DOWNLOAD_HELP` in `tests/test_app.py` and the README entry with it. Widget keys are namespaced by batch *and* index (`transcript_b{batch}_{i}`, `download_{txt,srt}_b{batch}_{i}`), reading `batch_id` from session state. The `{i}` avoids collisions across files in one batch; the `b{batch}` is load-bearing for correctness. **A keyed `st.text_area` restores its session-state value and ignores the `value` argument**, so a batch-invariant `transcript_{i}` renders the *previous* batch's text under the new batch's `st.subheader` — and since the **Download** button's payload is the text area's return value, it serves that stale text under the new `file_stem`. This needs no user edit to trigger: transcribe A, then B, and B's text never appears. Flipping `include_subtitles` does not rescue it either, because that changes only the download key (`_txt` → `_srt`) while `transcript_*` is untouched, so the SRT cues never render
- `_display_transcription` is a plain function but is invoked through `st.fragment(_display_transcription)()` at the call site (not a `@st.fragment` decorator) so transcript edits and downloads rerun only this section instead of the whole script (which would otherwise re-evaluate all four input tabs). Wrapping at the call site rather than decorating keeps the function directly unit-testable — the real `st.fragment` wrapper returns `None` without running the body when there is no script-run context (bare test mode)

### Lazy Container Execution

`st.tabs` and `st.expander` can skip computing hidden content via `on_change="rerun"` plus the container's `.open` property, and the general best-practices guidance recommends it for expensive work behind tabs and expanders. This API is present well below the declared `streamlit>=1.59` floor (verified against a 1.58.0 install: `st.tabs` already accepts `key` and `on_change`, and `TabContainer.open` exists), so it is not what the floor is holding up — `st.skeleton` is. **That guidance does not apply to the Advanced options expander.** Its three widgets are not expensive work — they are widget *declarations* whose return values (`decode_independently`, `time_range_input`, `keyterms`) are consumed unconditionally further down the script. Gating the expander body would leave those names undefined whenever the expander is collapsed, i.e. by default. Widget declarations are cheap; their values are load-bearing.

The tabs **are** gated, but at a finer granularity than wrapping the whole body. `st.tabs(..., on_change="rerun", key="input_tabs")` enables each tab's `.open` flag, and the YouTube and URL tabs guard only their **fetch** on it (`if youtube_tab.open and ...`), never the `st.text_input` that feeds it.

**Those fetches do not run in the tab body — only their preview slot lives there.** Each tab declares its `st.text_input` and then a bare `youtube_slot = st.container()` / `url_slot = st.container()`; the gated fetch itself sits further down the script, below every control, and writes back into that reserved container. Streamlit paints top to bottom and marks not-yet-redrawn elements stale, so a fetch at its old position greyed out the language selector, all three toggles, and Advanced options for the length of a download that can run to 500 MB on a 60-second timeout. Reserving the slot is what lets the fetch move without the preview moving with it — verified through `AppTest`'s element tree, where the audio element still resolves as `Tab > Block > audio` despite running lexically after the expander. The **Transcribe** button is the one thing that still waits, and unavoidably: its `disabled` state reads `audio_sources`, which is exactly what the fetch produces.

**Each fetch fills its reserved slot with an `st.skeleton` while it runs, and the spelling matters.** It is `with youtube_slot, st.skeleton(height=AUDIO_PREVIEW_HEIGHT):` — *not* `youtube_slot.skeleton()`. The skeleton's context-manager form does not redirect bare `st.*` calls into itself; they land in the **parent** container. So entering the slot first is what keeps the `st.audio` preview and both error alerts inside the tab, whereas `youtube_slot.skeleton()` alone would put the skeleton in the tab and strand everything else below the controls — the exact layout bug the reserved slot exists to prevent. `AUDIO_PREVIEW_HEIGHT = 40` is measured off the rendered player (`[data-testid="stAudio"]` reports `40px`), not guessed: matching it is the whole point, since a skeleton the same size as the element replacing it is layout-neutral by construction and a wrong value just trades one jump for a smaller one.

⚠️ **`st.skeleton` requires `streamlit>=1.59`, and adding it is why the floor moved off `>=1.58`.** It does not exist at 1.58 — bisected directly: absent in 1.58.0, present in 1.59.0. Note the bundled skill's `performance.md` and `best-practices.md` both recommend it without qualification, because those docs ship with the *installed* Streamlit (1.63.0) and describe its API, not whatever floor the project declares. **This repo deliberately keeps those two values different**, so treat any API the bundled references recommend as needing a floor check before use — `uv run --no-project --with 'streamlit==<floor>' python -c "import inspect, streamlit as st; print(inspect.signature(st.<name>).parameters)"` settles it in one command (check parameters, not just `hasattr` — most of what the 1.63.0 references add is a kwarg on a command that already exists at the floor). Its testid is `stSkeletonElement`, not `stSkeleton`, which is worth knowing before concluding it failed to render.

That split is load-bearing. Wrapping the whole `with` body in `if tab.open:` would stop rendering the text input while the tab is hidden, and Streamlit removes state for widgets absent from a run (`SessionState._remove_stale_widgets`) — so the typed URL would be silently cleared on every tab switch. Widget declarations are cheap and must always run; only the side effect gets gated.

Note that `on_change="rerun"` does not itself skip anything: `TabContainer.__enter__`/`__exit__` are plain passthroughs (Python cannot skip a `with` body), so the flag only enables `.open` tracking and makes tab switches trigger a rerun. Every `if tab.open:` guard is hand-written.

Three accepted consequences. Switching tabs now costs a server rerun. `youtube_audio` / `url_audio` are `None` while another tab is active, so a pasted URL alone doesn't enable **Transcribe** unless its tab is showing (upload and record sources stay sticky across tabs, so the button's enabled state is source-dependent). **That asymmetry — ephemeral remote sources versus sticky local ones — is also what made the old flat `or`-chain mis-dispatch**, and the two are worth reading together: this bullet accepts that a remote source can fail to *enable* the button from another tab, while the dispatch bullet above fixes the inverse, a sticky upload silently *outranking* the tab you are looking at. And because a tab switch is now a rerun, it can **interrupt an in-flight transcription** — previously tab switching was purely client-side and could not; this is why `_handle_transcription` publishes results progressively rather than once at the end. `key="input_tabs"` holds the active tab's *label* in session state; `AppTest` has no tab-selection API, so the tests drive tab switches by writing that key.

### Accepted formats

Audio: mp3, m4a, wav, opus

Video: mp4, mov, webm, mkv

**The list is short on purpose, and the order is meaningful.** Both tuples run most-likely-first.
`ffmpeg` decodes far more than these eight — the tuples are a *product* decision about what the
uploader advertises and accepts, not a decoder limit.

The forcing constraint is that `st.file_uploader` renders `500MB per file • MP3, M4A, …` inside a
single span styled `white-space: nowrap; overflow: hidden; text-overflow: ellipsis`
(`static/js/FileUploader.*.js`), and no `st.file_uploader` parameter suppresses or reformats it —
the two halves are fed by `max_upload_size` (or `server.maxUploadSize`) and by `type` respectively,
so the only lever on the format list's *length* is shortening `type` itself. Measured
in a running app at the `centered` layout's max width: the span gets **571px**, the size prefix
eats **92px**, leaving **~479px** for the format list at Source Sans 14px (~31–40px per entry).
The former 17-format list needed 546px and truncated mid-word; the current eight need ~265px.
`test_format_list_fits_the_dropzone_hint` guards this with a 60-character proxy for that budget.

Two rejected alternatives, both worth not re-deriving:

- **An `st.caption` restating the full list below the uploader.** Works, but adds a permanent line
  of chrome under the dropzone to compensate for a truncated label. Rejected on UI grounds
- **Shortening `type` to the MIME shortcuts (`type=["audio", "video"]`).** Renders as just
  `audio, video` — the frontend formatter strips the `/*` and skips the uppercasing it applies to
  bare extensions — and always fits. But `enforce_filename_restriction`
  (`streamlit/elements/lib/file_uploader_utils.py`) **skips server-side validation entirely** when
  any entry contains a `/`, because the backend can't tell whether a file was meant to match a
  MIME pattern or an extension. Bare extensions are what keep that bypass guard active

Dropping a format rejects it browser-side *and* server-side, so this is a real narrowing, not a
label tweak. It applies only to the Upload tab: the YouTube and URL paths never consult these
tuples, so a `.flac` URL still transcribes fine.

Formats removed when the list was cut to eight, in case one needs restoring: `aac` (near-redundant
— standalone `.aac` is rare, AAC audio almost always arrives inside `.m4a`), `flac`, `ogg`
(Telegram voice notes, where WhatsApp uses `.opus`), `aiff`, `avi`, `wmv`, `flv`, `mpeg`, `3gpp`.

If a newline ever seems like the fix for a long format string somewhere: Streamlit's markdown
pipeline ships no `remark-breaks`, so a single `\n` collapses to a space rather than breaking.

### Upload Limit

- Per-file cap of **500 MB**, set in `.streamlit/config.toml` via `server.maxUploadSize`
- Enforced browser-side *and* server-side — the upload route rejects an oversized body three ways (a `Content-Length` fast-fail, a cap on the streaming read, and an exact post-parse size check), all HTTP 413, so the cap is not client-bypassable. Each file is a separate PUT, so with `accept_multiple_files=True` the cap is per-file, not aggregate
- Server restart required after changing this setting

### Theme

- Streamlit's built-in light and dark themes — `.streamlit/config.toml` carries **no `[theme]` section at all**, so the app inherits the stock palette and typography
- Deliberately unconfigured rather than re-specified: every `theme.*` option defaults to `None` (the stock look lives in the frontend, not in config defaults), so writing out the default hex values would pin the app to one Streamlit version's styling instead of tracking upstream restyles
- The light/dark switcher in the app settings menu stays available. The "single-mode `[theme]` locks the app to one mode" hazard applies only to *custom* themes; with no theme configured, Light / Dark / system setting are all offered
- Native theming only (no custom CSS/HTML). If a custom theme is reintroduced, define both `[theme.light]` and `[theme.dark]` to keep the switcher, and note that font changes require a server restart

### Error Handling

- `RuntimeError` caught explicitly for transcription failures
- Unexpected exceptions shown with `st.exception()`
- **Nothing calls `st.error` directly — every error goes through `_error(message)`**, which applies `ERROR_ICON`, condenses, and escapes. All three are structural on purpose:
  - **The icon.** `st.error` has no default — its docstring is explicit that "if no leading icon is found, no icon is displayed", and the leading-emoji extraction it *does* perform never fires here because every body starts with a word. So an omission is a real visual difference, not a no-op. It used to be a literal repeated at seven call sites with six of them missing it; a helper makes a new bare-box `st.error` impossible rather than merely documented-against. **The same "no default icon" rule applies to `st.info`**, which has exactly one call site (the YouTube-URL-in-the-URL-tab redirect) and now passes `:material/smart_display:` explicitly. There is no `_info` helper because one call site does not need routing — but if a second ever appears, add one rather than repeating the literal, since that is precisely how `st.error` drifted to six bare boxes
  - **The escaping, and this is the part that is not cosmetic.** `st.error`'s body is one of the two sinks that render without the frontend's `isLabel` flag (see `_escape_markdown`), and **the exception text reaching it is untrusted, not just the filename**. `yt-dlp`'s `UnsupportedError` is literally `f"Unsupported URL: {url}"` and `YOUTUBE_URL_RE` is prefix-anchored, so `https://youtu.be/![](https://host/p.png)` matches the regex, fails extraction, and arrives in the alert body verbatim — firing an outbound request. `_validate_time_range` echoes the raw input back the same way (`f"Invalid time range: {token!r} …"`), and `HTTPError.__str__` embeds a server-controlled reason phrase
  - **The length cap, which exists because the visibility fix created the need for it.** `_condense` collapses the message to one line and caps it at `ERROR_MESSAGE_LIMIT = 240`, keeping the **head and the tail** rather than truncating. The exception text was never written for a UI: a corrupt file makes `mlx_whisper` raise `RuntimeError("Failed to load audio: " + ffmpeg's entire stderr)` — measured at **1740 characters over 14 lines**, of which roughly 1500 are ffmpeg's version banner and its `--enable-*` configure flags. That cost nothing while per-file errors rendered into a collapsed `st.status` nobody had reason to open; making them visible is what promoted it from invisible to a wall of text, so it is a consequence of that fix rather than a pre-existing wart. Both ends are kept because the two useful halves sit at opposite ends — the head carries `Transcription failed for <name>: Failed to load audio:` (what failed), the tail carries `Invalid data found when processing input` (why). Head-only renders the version banner; tail-only drops the filename and the operation. 240 was chosen by rendering 240/320/400 against the real failure, not picked
  - **`_condense` runs *before* `_escape_markdown`, and the order is load-bearing.** Escaping first would let the cut land between a backslash and the character it escapes, dropping the backslash into the head and releasing a live `[` or `:` into the tail. `test_error_condenses_before_escaping` pins it, and the observable signature is the ellipsis marker itself: inserted before escaping it renders escaped, inserted after it arrives raw — no boundary arithmetic required. A first draft of that test computed the cut's parity to land a backslash exactly on the boundary, which was fragile and unnecessary
  - `_error` escapes the **whole** message rather than the untrusted spans, so callers pass raw text and never escape at the interpolation site — which is what makes "no unescaped interpolation" checkable by grepping for `st.error(`. Escaping the fixed literals too is harmless: `:` becomes `\:`, which renders as a colon. The cost is that the four exact-match assertions in `tests/test_app.py` carry `\:` in their expected strings

### Testing

Mocked at the boundary (`mlx_whisper`, `yt_dlp`, `urlopen`, `st`). Shared fixtures (`mock_mlx`, `mock_st`, `mock_uploaded_file`) and helpers (`_make_file`, `_stub_urlopen`, `_stub_ytdlp`, `_stub_ytdlp_class`, `_make_transcription`, `_expected_transcribe_kwargs`, `_handle_transcription_kwargs`, `_ui_state`, `_assert_declared_mime`) factor the common setup; **`_stub_ytdlp` and `_stub_ytdlp_class` are two entry points to one stub** and neither is redundant — `_stub_ytdlp` patches `streamlit_app.yt_dlp`, which the `AppTest` cases cannot use since AppTest re-executes the script and rebinds its imports, so those patch `yt_dlp.YoutubeDL` directly and pass the class in. Both take `**info` so a case can set `vcodec`. `_make_file` returns a `MagicMock`, which is why the `seek(0)` test builds a **real** `UploadedFile` instead: a mock has no cursor and cannot show the defect; kwarg-forwarding cases are `@pytest.mark.parametrize`d. An autouse `_clear_caches` fixture clears the `@st.cache_data` wrappers before each test so cached results don't leak between cases. It also calls **`st.cache_data.clear()`**, which is not redundant: the per-wrapper `.clear()` calls only reach caches created by the *imported* `streamlit_app` module, while `AppTest` re-executes the script as a separate module with its own cache store that otherwise survives the whole pytest session. No assertion depends on it today — removing the global clear leaves the suite green, because the parametrize order runs `skipped_when_hidden` before `fetched_when_active`, so nothing has populated the AppTest-module cache by the time the hidden-tab case asserts. It keeps those cases order-*independent*: if that order ever flipped (reordering, `-k` selection, a randomizer plugin), the hidden-tab case would read a stale hit and its `call_count == 0` assertion would pass even with the `tab.open` guard removed.

The subsumption runs the other way too, which is the part that is easy to get backwards: `st.cache_data.clear()` is `CacheDataAPI.clear()` → `DataCaches.clear_all()` (`streamlit/runtime/caching/cache_data_api.py`), and `_data_caches` is a module-level singleton shared by every module in the process — so the global call already covers the three per-wrapper `.clear()` calls above. Those are kept as a readable inventory of which caches exist, not because the global clear misses them. **A new `@st.cache_data` function therefore needs no new line in this fixture.**

**That subsumption is scoped to `cache_data` and does not extend to `cache_resource`.** The two APIs own separate module-level singletons, so the fixture must call `st.cache_resource.clear()` as well — which it does, for `_fetch_youtube_audio` and `_fetch_url_audio`. Measured directly rather than inferred: with a `@st.cache_resource` counter function, `st.cache_data.clear()` left it cached and only `st.cache_resource.clear()` evicted it. **A new `@st.cache_resource` function needs no new line either — but flipping an existing function between the two decorators does, if it removes the last user of one of the two global clears.**

`mock_st` is **not** a bare `MagicMock`: it stubs `columns` to return a list sized from the spec (so `label_col, input_col = st.columns([3, 1])` unpacks) and `text_area` to echo its `value` argument straight back (which is the entire mechanism behind the download-reflects-edits assertions). Adding an `st.*` call whose return value is unpacked or fed onward inside a function under test means teaching this fixture about it first — otherwise the failure surfaces as a `MagicMock` unpacking error rather than a readable assertion. **The `columns` stub is currently unexercised**: no path in `streamlit_app.py` calls `st.columns` any more. It is kept precisely because the failure it prevents is unreadable, so anyone re-adding a split gets a working fixture instead of a confusing traceback — but do not read its presence as evidence that the app still uses columns.

`[tool.pytest.ini_options] pythonpath = ["."]` is what makes `from streamlit_app import ...` resolve — the project is not installed into the venv and declares no build backend, so `pytest` only works from the repo root (hence the `cd "$CLAUDE_PROJECT_DIR"` in the Stop hook).

- **`Transcript format` (`st.segmented_control`)** — three cases. `test_transcript_formats` pins the constant tuple and its order (index 0 is the default). `test_transcript_format_defaults_to_plain_text` pins `default=` through `AppTest`. `test_transcript_format_drives_include_subtitles` is the one that matters: `include_subtitles = transcript_format == FORMAT_SUBTITLES` is a bare module-level comparison whose only observable effect is the flag `_handle_transcription` stores, so the test drives the **whole** path — stub `urllib.request.urlopen` for a URL source to enable **Transcribe**, stub `mlx_whisper.transcribe`, set the control, click, and read the stored dict back. Parametrized over both options and mutation-checked: inverting the comparison fails both. Note `required=True` is *not* covered and cannot be — see the Input Modes bullet
- Format constants — `AUDIO_FORMATS` / `VIDEO_FORMATS` pinned exactly (order included, since it decides what survives if the dropzone label is ever cut), plus `test_format_list_fits_the_dropzone_hint`, a 60-character ceiling standing in for the dropzone's ~479px budget (see **Accepted formats**)
- `_transcribe` — defaults, kwarg forwarding (language, task, initial_prompt, no_verbatim, condition_on_previous_text, clip_timestamps), temp-file cleanup, empty-text guard; **the multi-clip split and merge**, in five cases — `0,60,120,180` must produce *two* calls carrying `"0,60"` and `"120,180"` (this replaced a `multi_clip` parametrize entry that asserted the whole string was forwarded, i.e. a green assertion on the broken behaviour), a trailing unpaired start stays open-ended, a single clip still makes exactly one call, the merge concatenates text and segments and takes the first clip's language, and the empty-text guard fires only when *every* clip is silent. Four of the five are mutation-checked; **`test_transcribe_single_clip_makes_one_call` is deliberately not**, and cannot be — one call is what *both* the old and new code produce for a single clip, so it guards the common path against a future change rather than pinning this one. Plus `_split_clips` parametrized over full-file/single-pair/two-pairs/trailing-start/whitespace/blank; `no_verbatim=True` flips `word_timestamps` and `hallucination_silence_threshold`; `clip_timestamps` defaults to `"0"` (full file) and accepts custom ranges (e.g., `"30,90"`)
- `_handle_transcription` — session-state storage, per-file error handling (RuntimeError + unexpected), kwarg forwarding, multi-file batches, partial-failure scenarios; **error *visibility*, which is a positional property and so has a positional assertion** — `test_handle_transcription_renders_errors_after_the_status_closes` reads `mock_st.mock_calls` and requires the `error` entry to come after `status().__exit__`, since rendering into the collapsed status is what made a failed file produce no on-screen explanation at all. The same case pins `state="error"` on the terminal update. Both halves are mutation-checked (moving the replay loop back inside the `with`, or pinning the state to `"complete"`, each fail it), and note no *content* assertion could have caught the original bug — the `st.error` call was always made, just into an invisible container; the two batch-level `st.status` labels (`test_handle_transcription_pluralizes_the_status_labels`, parametrized single/batch, asserting the opening string through `st.status(...)` itself and the closing one through the final `status.update(..., state="complete")` — these were the *only* two user-visible strings in the module with no assertion at all, which is how `file(s)` survived); **the `mx.clear_cache()` reclaim**, in two cases — a normal batch calls it once, and a batch interrupted by a `BaseException` still calls it, which is the half that pins `finally` rather than a call after the block (mutation-checked: a success-path-only call passes the first and fails the second); progressive publishing is pinned by two cases — a `BaseException` raised mid-batch (standing in for `RerunException`) leaves the already-finished files in session state, and a batch where every file fails clears the previous batch's results; a third asserts `batch_id` advances on every batch **including an all-failed one**, since a stale text area would otherwise outlive the results it was rendered from. Markdown escaping of the filename is pinned at all three interpolation sites: a parametrized case covers both `st.error` branches (`RuntimeError` and unexpected) and a separate case reads the `st.status` label off `mock_st.status.return_value.__enter__.return_value`, since that one renders on the success path where no error is raised. Mutation-checked — dropping the `_escape_markdown` call fails all three. A fourth case pins the **whitespace collapse** at the `st.error` sink: a filename carrying `\n\n# Big\n\n> quote` must arrive as one line, and it asserts `#` and `>` survive as *literal* characters, since losing the line start is what defuses them rather than escaping. `test_display_transcription_collapses_whitespace_in_subheader` pins the same property at the **heading** sink — a separate case because that is a separate renderer, and because an earlier version of this file wrongly claimed the heading was already protected. Both are mutation-checked, along with two `_escape_markdown` cases: reverting the helper's last line to `sub(r"\\\1", text)` fails all four
- **A `seek(0)` regression test.** `test_handle_transcription_rewinds_the_cursor_before_reading` builds a *real* `UploadedFile` (the shared `_make_file` helper returns a `MagicMock`, which has no cursor and cannot show this) and runs `_handle_transcription` over the same object twice, asserting `_transcribe` received the full bytes both times. Without the `seek(0)` the second call gets `b""`. It pins a same-run double read; it would *not* have caught removing the Record tab's `st.audio` preview, as this line used to claim, because no rerun path hands the script an exhausted object (see the `seek(0)` bullet under **Input Modes**)
- `_transcription_kwargs` — UI-flag → `_handle_transcription` kwargs mapping; `translate=True` ↔ `task="translate"`, `decode_independently=True` ↔ `condition_on_previous_text=False`; passthrough of `language`, `include_subtitles`, `initial_prompt`, `no_verbatim`, `clip_timestamps`
- `_display_transcription` — filename subheader, editable text area, **Download** button alone in a right-aligned horizontal container (the `horizontal_alignment="right"` value is asserted, since `"distribute"` would left-align a standalone child and silently unstick the shared edge); label is always `Download` with a `:material/download:` icon, `on_click="ignore"`, and `width=SELECT_WIDTH`; filename, MIME type, and widget key are derived from `include_subtitles` (`.txt` vs `.srt`); `help=` tooltip preserved in both `.txt` and `.srt` paths (pinned against the module-local `DOWNLOAD_HELP` constant, spelled out rather than imported from `streamlit_app` so the assertion is not tautological); each result wrapped in its own `st.container(border=True)`, asserted by counting `call(border=True)` across a two-result batch so a single shared box would fail; **edits to the text area flow through to the download payload — but only as far as the mocked `st` can see.** `mock_st` echoes `text_area`'s `value` straight back, so that assertion pins Python-level *forwarding* and structurally cannot observe that the real `st.download_button` bakes its payload at render time (see `_display_transcription` above). Do not read a green suite here as evidence that an uncommitted edit reaches the file; multi-file stacked rendering (per-file widget counts and subheaders — the `transcript_b{batch}_{i}` / `download_{ext}_b{batch}_{i}` keys are pinned only at index 0 and batch 0, so a key that lost its `{i}` would still pass); a dedicated case renders at `batch_id = 7` to pin the namespace itself; filenames with Markdown-special characters are escaped in the subheader. Tested as a plain function (the `st.fragment` wrap is applied only at the call site, so the body runs directly under the mocked `st`)
- `_RemoteAudio` / `_fetch_youtube_audio` / `_fetch_url_audio` — adapter round-trip; YouTube fetch with mocked `yt_dlp` (bytes + filename, `extract_info` call args, safe-download options `format=bestaudio/best`, `noplaylist`, `restrictfilenames`, `quiet`, `max_filesize`, plus an oversized on-disk file raising `RuntimeError` — the two size mechanisms are asserted separately, since `max_filesize` alone never fires on a fragmented download); URL fetch with mocked `urlopen` (bytes + filename, `timeout=60`, query-string stripping, percent-decoded filename, empty-path fallback to `download`, oversized-response `RuntimeError`). Both size tests patch `streamlit_app.MAX_DOWNLOAD_BYTES` down to 10 rather than building a 500 MB fixture. Each fetch's **third** return value — the mimetype — has its own cases: the URL one is parametrized over server-`Content-Type`-wins, parameter stripping, case-insensitivity, no-extension-to-guess, and the three fallbacks (`text/html`, `application/octet-stream`, absent header); the YouTube one covers `vcodec="none"` → `audio/webm` and `vcodec="vp9"` → `video/webm`. **`_stub_urlopen` must set `response.headers` to a real `dict`**, which is why it takes a `content_type` argument: on a bare `MagicMock` every string method returns a truthy `MagicMock`, so `declared.startswith(("audio/", "video/"))` would pass and hand `st.audio` a `MagicMock` as its `format`. **`test_yt_dlp_discovers_the_bundled_deno_runtime` is the one YouTube case that does *not* mock `yt_dlp`**: it asserts the `deno` extra put a binary in the venv's scripts dir (`shutil.which("deno", path=sysconfig.get_path("scripts"))`) and that a real `YoutubeDL` resolves it there (`_js_runtimes["deno"].info.supported`, `info.path` equal to that binary), spawning `deno --version` once with no network. Nothing else can see the runtime — drop `[deno]` from `pyproject.toml` and the rest of the suite stays green while every fetch falls back to the JS-less client. Mutation-checked: with the extra removed and the venv re-synced it fails on the first assertion
- Formatting helpers — `_plural` (singular at 1, plural at 2, and **plural at 0** — the zero case is the one an `n != 1` implementation gets right and an `n > 1` implementation does not; mutation-checked, collapsing the helper to an unconditional `f"{count} {noun}s"` fails it *and* the single-file status-label case), `_format_language` (`None` → `"Detect"`, title-casing of lowercase codes), `_format_timestamp` (zero, minutes/seconds, hours, comma decimal marker), `_format_srt` (single-segment, multi-segment cue separator, `-->` escaping to `->` to keep SRT structure intact, **cue wrapping** end-to-end, and **blank-segment dropping** — a batch mixing real text with an empty and an instantaneous segment must yield two blocks numbered 1 and 2, which pins both the filter and the contiguous renumbering; mutation-checked), `_wrap_cue` (short passthrough, balanced two-line, embedded newlines, whitespace; plus a balancing case and a never-split-a-token case). **The balancing case names a specific sentence and the comment says why**: most sentences wrap identically greedy and balanced, so an arbitrary long string makes that assertion pass without testing anything — the first draft of the test used one and did exactly that. `The internationalization committee reconvened.` gives 34/11 greedy against 24/21 balanced. All four wrapping properties are mutation-checked, `_escape_markdown` (plain passthrough, underscores, brackets, all special chars, backslash, plus a named and a numeric **HTML character reference** — `Rock &amp; Roll.mp3` and `clip&#58;streamlit&#58;.mp3` — which are what the `&` in the class exists for; mutation-checked, dropping `&` fails all three of `all_specials`, `named_entity`, `numeric_entity`), `_media_mime` (**all eight** upload formats — the list shipped missing `mov` and `webm`, so dropping either map entry silently restored the `audio/wav` default with a green suite, and `webm` is the single most likely extension on the YouTube `bestaudio` path — plus `flac`, an uppercase extension, an unknown extension, and the extensionless `download` fallback — the last two both reachable, since the remote paths never consult `AUDIO_FORMATS`/`VIDEO_FORMATS`), `_validate_time_range` (blank/single/multi/whitespace/decimal/trailing-start valid cases → `None`; non-numeric, negative, end ≤ start, out-of-order, trailing/empty token → error message; plus **four non-finite cases** — `nan`, `inf`, `0,inf` and the overflowing literal `1e400`. `float()` accepts all four and *every* comparison against a nan is False, so `value < 0` and `end <= start` both passed and the input reached `mlx_whisper` to fail there instead. Guarded by `math.isfinite` and mutation-checked). Odd counts are valid — a trailing unpaired value is a start that runs to the end of the file, matching Whisper's `clip_timestamps`
- **Source dispatch (`AppTest`)** — `test_transcribe_dispatches_from_the_active_tab` seeds the uploader *and* a remote source, then asserts which one `_handle_transcription` actually recorded. Parametrized over two cases that are deliberately **not** symmetric: `active_tab_wins` (URL tab open, upload still loaded → the URL must win) is the mutation check and fails the moment the `tab.open` filter is dropped; `empty_tab_falls_back` (Record tab open and empty, upload loaded → the upload runs) passes both before and after the fix by design, because it guards the fallback rather than the fix — it exists so a later "only the open tab counts" simplification cannot silently make **Transcribe** dead. Nothing covered dispatch selection at all before, which is why the whole 146-test suite stayed green with the bug in place
- `test_youtube_runtime_error_renders_an_alert` — the 500 MB stat gate raises `RuntimeError`, which missed the `DownloadError` branch and fell through to the generic handler. `assert not at.exception` is the discriminating assertion: the wrong branch attaches a traceback via `st.exception`, so a message-only check would not separate them
- Tab-gated fetches (`AppTest`) — both remote tabs are parametrized over hidden/active: a URL typed while another tab is active performs no fetch, the same URL with its own tab active fetches exactly once, and the typed value is retained either way (proving the widget is not gated, only the fetch). A third case asserts an active remote tab enables **Transcribe**. The active tab is set by writing `session_state["input_tabs"]` (AppTest can't click tabs), and the network entry points are patched on `urllib.request` / `yt_dlp` rather than on `streamlit_app`, because AppTest re-executes the script each run and rebinds its imports. Both gates are mutation-checked: deleting either `tab.open` guard fails the corresponding hidden-tab case
- **`st.audio`'s `format=` wiring, pinned through the media URL** — `test_media_mime` and the `_fetch_*` mimetype cases cover *which* type is chosen, but none of them can see whether it is ever **passed**: with `format=` deleted from all three `st.audio` sites the whole suite still passed, and every preview was silently served as WAV again. The two `*_preview_renders_inside_its_tab` cases carry that assertion (they already resolve the same audio element, so it costs no extra `AppTest` run) via the shared `_assert_declared_mime` helper. It reads `proto.url`'s extension, since the mimetype itself is not in the proto — and it **computes** both the expected extension and the WAV baseline with Streamlit's own `get_extension_for_mimetype` rather than hardcoding `.mp3`/`.m4a`, because that lookup is `mimetypes.guess_extension`, the same platform-varying table `MEDIA_MIME_TYPES` exists to avoid. Its `expected and expected != <wav suffix>` guard is what stops the assertion passing vacuously on a host whose tables collapse the two. Mutation-checked — dropping `format=` fails both. The **Upload** tab's call site stays uncovered, but **not because it is uncoverable — that claim was stale and is corrected here.** This file long asserted `AppTest` had no way to seed a `file_uploader`; since at least the 1.59 floor (checked directly against 1.59.0) `FileUploader.set_value` takes `(name, bytes, mime)`, or a sequence of those under `accept_multiple_files=True` (`streamlit/testing/v1/element_tree.py`, with the accessor exposed as `at.file_uploader`), and `test_transcribe_dispatches_from_the_active_tab` uses it. Adding the third `_assert_declared_mime` is now a small job rather than an impossible one. It is present at the floor with the same signature, and CI runs the locked 1.63.0, so the existing use is safe at both ends of the range
- **`_error` is tested directly**, parametrized over a yt-dlp-shaped exception string carrying `![](…)` and a `_validate_time_range` message carrying the same, asserting the icon kwarg and that no live image syntax survives. Mutation-checked — dropping the `_escape_markdown` call inside `_error` fails **11** cases: 8 under mocked `st`, the `AppTest` case `test_youtube_runtime_error_renders_an_alert` (whose expected string carries the escaped `\:`), and the two condensing cases that assert on escaped output. **This count is a maintenance trap and has now moved twice** — 8 until the YouTube `RuntimeError` test, 9 until `_condense` arrived, 11 now. Any new exact-match assertion on an `_error` message moves it again, so re-measure rather than reason: mutate the call and read the failure count
- Module UI (`streamlit.testing.v1.AppTest`) — runs the real script (not mocked `st`, loaded via an absolute path so it's cwd-independent) to cover the module-level UI the mocked tests can't reach: clean render + page title, the four Material-icon tab labels, the **Transcribe** button's `:material/graphic_eq:` icon and disabled-without-audio state (**`AppTest` at the locked 1.63.0 raises `AppTestError` on any interaction with a disabled widget** — `Widget.set_value`, which `Button.click` routes through, calls `_assert_can_interact` in `testing/v1/element_tree.py`; absent at 1.62.0, present at 1.63.0. So a case must enable **Transcribe** — seed an upload or a remote source *and* leave **Time range** blank or valid — before `at.button[0].click()`; reading `.disabled` is an attribute read, not an interaction, and stays fine. Under 1.61.1 the same click was a silent no-op, since the server discarded the forged trigger value, so a case clicking a dead button passed vacuously; it now errors, which is the better failure), the time-range input's inline `st.error` on invalid input (and no error on valid input, asserted end-to-end through the rendered UI), and (with `st.session_state["transcription"]` seeded) the fragment-rendered subheader + text area + **Download** button carrying its `:material/download:` icon; the empty-state case renders no download button. Two cases publish **twice** with different data (`_publish` mirrors `_handle_transcription`'s results-plus-`batch_id` write) to pin the batch-namespaced keys: the second batch's text must replace the first's, edits must still stick *within* a batch, and the same must hold when `include_subtitles` flips between the two. Staleness is a two-render property — no single-render test can see it, which is why the original bug survived the suite. Mutation-checked: reverting the keys to `transcript_{i}` fails both. A plain unit test asserts the `PAGE_CONFIG` constant (`set_page_config` args aren't introspectable via AppTest)

## Resources

- [mlx-whisper](https://pypi.org/project/mlx-whisper/)

## License

MIT (see `LICENSE`); declared via `license = "MIT"` / `license-files = ["LICENSE"]` in `pyproject.toml`.
